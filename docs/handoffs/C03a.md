# C03a — remapped confirmation and checkpoint continuation

Status: **implemented and checked in the main playable scene** on 24 September 2026 with Godot 4.6.1 stable Mono, Compatibility renderer. This follows the C04 checkpoint review's F14/F15.

Focused native table controls now receive `ui_accept` before table inspect/bin shortcuts. Grid focus keeps the existing table actions. The Load list and pause picker show the same Checkpoint 1–3 names, saved time and completed/required progress. Loading a manual checkpoint selects that slot for Save and Save and quit; a new run or autosave load selects Checkpoint 1. SaveService still owns A/B recovery and failed-write retention.

| Main-scene setup and action | Observed result |
| --- | --- |
| Real S1 table with 180 bagged cans; remap confirm to Y while Y is table inspect; focus Unload and press Y through `Input.parse_input_event` | The bag unloads once into cells 020–199. The inspect overlay does not open. Returning focus to the grid retains its own table shortcuts. |
| Loaded S1 table with a valuable in its tray and a partial glass bin; press Y on focused Sell, then remap confirm to left shoulder and press it on focused Seal and Exit | The valuable sells once, the partial bin seals once, and Exit returns to world without losing ownership. Mouse drag, grid sort/correct, pause/resume and settings return also pass in the same scene check. |
| Save Checkpoints 1 and 2 with different table states; load Checkpoint 2, pause and confirm Save with controller A | Picker remains on Checkpoint 2; its generation advances while Checkpoint 1's generation stays unchanged. |
| Save Checkpoint 3, load it, move the player, then confirm Save and quit | Checkpoint 3 advances, Checkpoint 2 stays unchanged, and the title Load list displays checkpoint names, time and progress. Another run's Checkpoint 2 remains isolated. |

Checks: `tests/validate_sorting.gd` exited 0 (`failures=0`) after the final content-7 change. `tests/validate_save.gd` exited 0 (`failures=0`) in a fresh isolated `%APPDATA%`; its blocked-directory error is the intentional failed-write case. No new scene or test framework was added. Physical controller acceptance remains C09.

The accelerated completion check also loaded a run before the pause picker had ever been opened. That exposed an empty-picker selection error; `BeachMain.load_run` now populates the native picker before choosing the loaded slot. The rerun of `tests/validate_full_run.gd -- --final-special` exited 0 with no picker error, 5,700/5,700 required objects, one immutable receipt and two reloads.
