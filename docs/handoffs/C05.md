# C05 handoff — catalog and clutter work in progress

## Opening loop and S1 access — 24 September 2026

The first scripted normal-input opening pass found a service blocker: after collecting 20 nearby waste items on foot, the player could not enter S1 because the four outdoor containers stood across the hut doorway. The containers at all three huts now leave the centre path clear. S2's pair on the shop side moved farther left after the [shop-frontage capture](images/C05-v4/service-s2.png) exposed a conflict; see also [S1 after the move](images/C05-v4/service-entrance.png) and the [C04 service layout handoff](C04.md).

On the content-8 `first-shore` main scene with an isolated profile, a temporary play probe started at the actual new-run spawn and used normal controller movement and primary input to fill two 20-item starter bags. It walked out to S1 and back between cycles, aimed at the tabletop and entered it through the normal interact input, then unloaded and sorted each bag through focused UI activation. The second bag and table pass completed without teleporting the player; 40 required waste items ended in four correctly categorised sealed bags. Sealing, rack removal, container deposit and collection were accelerated through the existing services for this probe. Collection credited all 40, paid $80 and left zero state-invariant errors. The probe used 1,878 physics frames (about 31 seconds of simulated frames) because target selection and UI input were scripted; this is not a human-paced timing measurement. The temporary probe script was removed after the run.

`tests/validate_world_traversal.gd` now starts outside each service bin line and entered all three huts with normal movement. After S2's pair moved left, the same real-scene check walked to the equipment shop counter and opened its UI with normal interact input. `tests/run_checks.gd` and the 5,400-waste/300-prop completed-state slot/mesh audit exited 0 after the S1/S3 entrance move. **C05 remains open** for human-paced opening cycles, dense patch/tideline/underwater visibility and air-limited pickup after final C04/C07 geometry.

### Normal sealed-bag trip — 24 September 2026

Setup: the playable `scenes/main.tscn` scene with a fresh `first-shore` run and isolated profile. A temporary scene probe used the real ItemStore and S1 table/bin to stage one PMD item, then focused the table's PMD and Seal controls and activated them with controller A input. It placed the player outside S1 and used normal movement through the doorway and around the right side of the table to reach the output rack. Aimed primary input picked up the sealed bag; the player carried it back through the doorway, aimed at the matching PMD container and pressed E, then walked to the collection hotline and pressed E. The record changed RACK → HELD → CONTAINER → COLLECTED. One receipt paid $2 for one correctly sorted item, and state validation found zero ownership errors. Direct diagonal travel from the centre aisle to the rack is blocked by the table; the playable route around its right end is clear. The temporary probe was removed.

The existing main-scene traversal check now keeps one PMD bag on that normal-input rack/container path and calls collection through normal interaction after the other two category bags are deposited by service. It checks the wallet against the receipt's base pay plus sorting bonus. `tests/validate_world_traversal.gd` exited 0 on the final run: 13 route waypoints, three hut entries, shop access, two reef lanes, six new catalog pickups, the S1 bag trip, collection payment, a pier carry and zero failures. This is a bounded regression for the doorway and rack access. The remaining C05 evidence still needs human-paced cycles and on-foot visibility/air review; a scripted one-item trip does not establish those.

## Current result — 24 September 2026

The playable `scenes/main.tscn` run now includes six named waste forms: straw, plastic wrap, empty drink carton, fries, hamburger and sealed oil container. Their `ItemDefinition` resources use PMD, organic and general categories respectively, and each uses an explicit compact labelled placeholder scene. The asset register records A12C for the carton as well as the five previously named roles. Final models remain missing.

The generator remains at 5,400 required waste, 300 required props, 40 optional valuables, 300 buried waste, 24 rescue attachments, 120 residue items and 60 dirty props. It keeps the same section/category quotas and stable ownership. `beach-content-8` and beach version 8 identify this new manifest. The content-7 checkpoint is commit `92fb167`; older saves are not migrated into this changed layout. For `manifest-fixture-v1`, the current content hash is `4712283664ce031103f232807f8c9348d7601db1c1783999b9cdfd1ebcefa60f` and manifest hash is `6d6a84fa1b520dfa9603e2900cf1534169b37953a01755825a41be6ccd6f8622`.

To break up the fixed grid, loose small waste receives bounded 150 mm horizontal jitter and each section draws cells from two broad seeded cleanup patches. Existing pile assignment remains at approximately 25% of eligible dry waste. The generator skips the three service footprints and two lifeguard platforms, keeps buried finds clear of the pier deck/head, and seeds the west-shallows rescue sites farther from the rising shore floor. These are authored bounds in `scripts/world/manifest_generator.gd`; later changes to the corresponding scene geometry must update them together. Small-item labels are readable up close and culled at five metres rather than filling the wide view.

## Playable and rendered evidence

The current content-8 new-run spawn is at `(-73, 0.02, 7)` with a 20-item starter bag. A read-only probe of the real generated `first-shore` main scene found 30 WORLD waste items within 5 m, 89 within 10 m and all 80 `arrival:start` required waste records present. The nearest six are 1.0–2.5 m away across cans, cups, a towel and a glass jar. This establishes available starter material near the player; it does not verify 40 normal pickups, two paid bag cycles or pickup visibility in the dense patch, which remain C05 work.

All commands used Godot 4.6.1 with a fresh process-only `%APPDATA%` path. `tests/run_checks.gd` exited 0 (boot, asset closure, ownership, input bindings, ten-seed manifest and pinned hashes). `tests/validate_world_traversal.gd` exited 0 in the actual main scene with 13 route waypoints / 147 m, three hut entries, two reef swims and zero reef overlaps among 1,010 reef objectives. It collected all six new forms with aimed normal primary input, unloaded them at S1, sorted by their definitions, sealed and deposited category bags, called collection and found no ownership invariant errors. It also collected a reef can near the old F13 obstruction and carried a real chair on and off the pier. This is normal-input gameplay with staged player positions, not a human-paced walk of every item.

`tests/validate_buried.gd` exited 0: all 340 finds have a tagged dig surface and clear reveal pose, and normal detector/stick handling, sorting and valuable sale passed. `tests/validate_rescue.gd` exited 0: all 12 sites / 24 original attachments are reachable and cut, including a full-bag dropped piece later collected by aimed stick input. Its moving-item aim check now updates the camera after the physics frame. `tests/validate_content_economy.gd` exited 0 across three seeded purchase proofs (minimum dry count 3,321; access package $490; 3,960 reachable purchase sets). `tests/validate_save.gd` exited 0 for initial/manual saves, recovery, rack, pose, table and save-and-quit. Its intentionally blocked write printed the expected directory error while preserving the prior save.

The new content-8 `tests/validate_full_run.gd -- --audit-slots` run also exited 0: all 300 occupied prop targets across 15 families resolved through real physics rays, no placed collider pairs overlapped, and the same 5,400 waste / 300 prop accelerated completion produced one result and receipt after two reloads. The audit uses short teleports to candidate approach points, so it remains bounded evidence of target clearance rather than proof of walking to every slot.

The [C05-v3 camera pair](images/C05-v3/capture-log.txt) rendered in Compatibility at 1920×1080, FOV 85, with the same five initial/restored camera transforms. The accelerated full-run setup placed all 300 props, sorted and collected all 5,400 required waste, produced one receipt, reloaded twice and ended with zero state-invariant errors. The [sports eye](images/C05-v3/sports-eye-initial.png) shows clutter concentrated around activity pockets with fewer prominent grid rows, while the [along-shore view](images/C05-v3/along-shore-initial.png) still exposes the broad bare foreground and [aerial view](images/C05-v3/aerial-initial.png) still shows C04's angular outer sand tips and hard inland edge. The six placeholders are small but visibly provisional. These captures do not establish the final visual rubric or a human-paced opening cycle.

## Open gate and next work

**C05 remains open.** The opening two starter-bag cycles and a dense patch need a paced normal-control pass. Inspect representative piles, tideline and underwater work sites on foot for visibility and air-limited pickup, then recheck after C04's remaining landform/approach changes and C07's plant geometry. The current density is still visually heavy; tune the patch shape against the reference without reducing the locked quotas. C09 owns the final 100-seed and exported-run checks. C04 remains open for the along-shore foreground, inland edge, occupied mesh/approach review and full composition acceptance. C06–C09 are not claimed complete by this handoff.
