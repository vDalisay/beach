# C08 checkpoint — first-person carry fit

## Beach hand views — 24 September 2026

At 1280×720, the two small carried props floated above the palms, and the original chair presentation covered the aim point at 70° FOV. The existing small-prop sockets now sit 0.14 m lower. The large-prop socket sits lower and to the right, and its presentation scale is 0.4 instead of 0.65. The chair remains visible while the center of the view stays clear. This is presentation only; world meshes, collision, reach, hand cost and save state are unchanged.

Setup/actions/observed: Godot 4.6.1 Mono Compatibility renderer, `scenes/main.tscn`, seed `first-shore`, 1280×720, isolated profile, development PC with RTX 3070. A one-off scene probe started a real run, set FOV to 70° and 110° through `SettingsStore`, staged a world bucket and beach ball in the two hands using `ItemStore.try_hold`, released them with the normal carry transaction, then staged a world beach chair. The probe refreshed the normal hand visuals and captured the game viewport after physics/render frames. All three holds and the release succeeded; the final run had zero state-invariant errors. This is rendered visual evidence with direct service setup, not controller pickup or player-paced play.

Final captures: [default 70°](images/C08-v1/default-70.png), [default 110°](images/C08-v1/default-110.png), [two small 70°](images/C08-v1/two-small-70.png), [two small 110°](images/C08-v1/two-small-110.png), [chair 70°](images/C08-v1/large-70.png), [chair 110°](images/C08-v1/large-110.png). The beach target area is visible around the chair at both limits. The bucket and ball meet the open palms more closely; the 70° chair's near legs still extend below the bottom edge, as do the forearms.

Validation after the fit change: `tests/validate_carry.gd` exited 0 (`hands=2`, `large_cost=2`, `failures=0`), and `tests/validate_ui.gd` exited 0 (`hud=1`, `booklet=4`, `pause=1`, `guidance=1`, `failures=0`). Both used Godot 4.6.1 with separate temporary `%APPDATA%` profiles. These checks cover ownership and UI behavior; the captures cover the main-scene composition.

## Equipped tool views — 24 September 2026

The same main-scene visual pass at 70° and 110° found the cloth placeholder filling much of the lower-right view and the Synty knife floating above the palm. The existing `ProgressionService.refresh_tool_visual` now scales and offsets those two scenes. The cloth and knife sit near the right hand without covering the central aim area. Stick, detector, sand cleaner and vacuum retain their previous transforms.

Setup/actions/observed: Godot 4.6.1 Mono Compatibility renderer, seed `first-shore`, 1280×720, isolated profile. A one-off scene probe staged ownership and active equipment for each of the six handheld tools in a real `RunSession`, refreshed the normal tool visual and context label, then captured both FOV limits. It did not purchase or equip at the physical shop; the final state-invariant check reported zero errors. The [70° tool views](images/C08-tools/cloth-70.png) and [110° cloth](images/C08-tools/cloth-110.png), [70° knife](images/C08-tools/knife-70.png) and [110° knife](images/C08-tools/knife-110.png) show the adjusted grips. The [detector](images/C08-tools/detector-70.png), [sand cleaner](images/C08-tools/sand_cleaner-70.png) and [vacuum](images/C08-tools/vacuum-70.png) remain below/right of the aim point at the tighter FOV. `tests/validate_tool_filters.gd` exited 0 with sand/vacuum eligibility and upgrade checks passing.

**C08 remains open.** Disposal-bag grips, crouch, movement and underwater transitions still need direct visual review. The 720p/1080p/wide/offscreen-4K UI evidence in P27 remains historical; physical TV/controller readability, full remapping review and human-paced early/middle/late play remain unverified. Required final art in `docs/asset_requests.md` is still unavailable.
