# C06 handoff — underwater palette checkpoint

## Clearer beach sky — 24 September 2026

The duplicated upper Synty cloud ring is removed. The lower ring keeps some faceted clouds, while the [matched player-height shore view](images/C06-cloud-single-trial/along-shore-restored.png) has a clearer central sky than the [two-ring baseline](images/C04-v64/along-shore-restored.png). The [new aerial view](images/C06-cloud-single-trial/aerial-restored.png) retains the bay, lighthouse and skyline. The remaining ring still has conspicuous angular strips, so this is a partial cloud-composition change rather than C06 visual acceptance.

Setup/actions/observed: Godot 4.6.1 Compatibility, `full-run-integration` in the playable main scene, fixed 1920×1080/FOV 85 review cameras, initial and accelerated restored states. The paired run completed 5,400 waste and 300 props, issued one result/receipt, reloaded twice, and exited 0 with no state-invariant failures. `tests/validate_pier_art.gd` also exited 0 in headless mode after checking the one scenic cloud layer and unchanged pier/coast collision. The cloud node has no gameplay collision or save state.

## 720p reef HUD view — 24 September 2026

The [1280×720 restored swimmer view](images/C07-branch-720c/P21-reef-after.png) uses the live HUD and current underwater treatment in `scenes/main.tscn`. The progress, bag/tool, air and jump/dive labels remain legible against their dark panels; the [same-camera dirty view](images/C07-branch-720c/P21-reef-before.png) retains visible nearby litter. This checks one fixed reef angle at normal UI scale and FOV, not every waterline, light, UI scale or extreme-FOV condition. The capture run ended with zero wildlife and ownership invariant failures after the input edge fix recorded in [C08](C08.md). C06 remains open for the wider water boundary, full underwater visual treatment and broader readability pass.

## Controller-input surface crossing — 24 September 2026

The existing main-scene traversal now includes one real waterline loop at world X 0 using mapped controller actions. From dry sand at Z 26, left-stick movement entered the swim volume near Z 45. B dived to feet Y about -1.09, switched the camera to the submerged state and consumed starter air. A surfaced the camera, and left-stick movement returned to dry sand near Z 33.93; after 75 physics frames, air had refilled to 10.00 s. The first probe established that walking into water alone correctly floats the player at the surface and does not consume air; the retained check explicitly dives. This is a small regression for the air/surface invariant in the actual level, not a paced human or visual HUD acceptance pass.

`tests/validate_world_traversal.gd` exited 0 on the final `first-shore` run in Godot 4.6.1 headless with an isolated profile. It also retained the 147 m walk, three hut entries, shop access, two reef lanes, 1,010 reef objective origins clear of rock collisions, six catalog pickups, paid S1 bag trip and pier chair carry. No gameplay swim logic or water collision was changed by the C06 material pass.

## Broad shallows blue pass — 24 September 2026

The water material's `deep_color` is now `(0.30, 0.53, 0.60)` instead of `(0.44, 0.63, 0.64)`. This is the colour that carries the broad 2–8 m-deep surface; the `shallow_color`, transparency, foam, shoreline mesh and authoritative swim level are unchanged. A first trial lowering only `shallow_color` barely changed the matched aerial because that parameter covers only the narrow edge, so it was reverted. In the fixed [before aerial](images/C06-v22/aerial-initial.png) and [after aerial](images/C06-v24/aerial-initial.png), the bay reads bluer while the seabed and floating items remain visible. At one stable water pixel `(1000,780)`, displayed RGB changed from `(147,236,248)` to `(118,212,239)`. The [player-height shore](images/C06-v24/along-shore-initial.png) keeps the pier and waterline legible; the [pier overhead](images/C06-v24/pier-overhead-initial.png) keeps nearby debris visible through water. The [reef view](images/C06-v24/reef-initial.png) still shows the unresolved flat habitat and blue fog.

Setup/actions/observed: Godot 4.6.1 Compatibility, seed `full-run-integration`, fixed 1920×1080/FOV 85 cameras in the playable main scene, initial and accelerated restored states. The run completed 5,400 waste, 300 props, one result/receipt, two reloads and zero state-invariant failures ([capture log](images/C06-v24/capture-log.txt)). A 600-frame water-facing 1080p sample at `(0,2.1,45) → (0,0,180)` on Ryzen 5 5600 / RTX 3070 measured 6.06 ms average, 6.44 ms p95, 1.54 ms average physics and 2,605 maximum draw calls. The earlier unmodified water-view sample was 6.06/6.47 ms average/p95 and 2,608 draw calls; this is within short-run variation, not a target-hardware result. C06 remains open for shoreline/offshore continuity, underwater surface treatment, readable HUD and normal-input surface crossings.

## Shaded cloud facets — 24 September 2026

The two Synty cloud rings now use normal StandardMaterial3D lighting with albedo `(0.87, 0.91, 0.94)` instead of an unshaded near-white override `(0.95, 0.98, 1)`. Their transforms, lack of shadows, and world placement are unchanged. In matched [before](images/C06-v20/along-shore-initial.png) and [after](images/C06-v22/along-shore-initial.png) player-height views, the previously flat white strips gain blue undersides and distinct bright upper facets. The [new aerial](images/C06-v22/aerial-initial.png) retains the bay and skyline framing, and the [restored shore](images/C06-v22/along-shore-restored.png) keeps the occupied seating and lighthouse readable. The large ring silhouette remains conspicuous near the top of the frame, so cloud composition is improved but not fully accepted.

Setup/actions/observed: Godot 4.6.1 Compatibility, `full-run-integration` in the playable main scene, fixed 1920×1080/FOV 85 cameras, initial and accelerated restored states. The run completed 5,400 waste, 300 props, one result/receipt, two reloads and zero state-invariant failures ([capture log](images/C06-v22/capture-log.txt)); no cloud collision, objectives or save records changed. A 600-frame dense shore sample on Ryzen 5 5600 / RTX 3070 measured 7.89 ms average, 13.99 ms p95, 1.34 ms average physics and 4,940 maximum draw calls. The earlier unshaded sample was 7.94/14.17 ms average/p95 and 4,940 draw calls; this short comparison does not establish a performance gain. Target-hardware acceptance remains open. A separate darker procedural-sky ground-colour trial created a dark wall behind the beach in the fixed aerial camera and was reverted.

## Shore-edge wedge cleanup — 24 September 2026

The 21 imported `SM_Env_Sand_Edge_02` instances produced two stark white wedges beside the pier in the [previous overhead view](images/C04-v63/pier-overhead-restored.png). A matched diagnostic capture with only that decorative MultiMesh hidden removed both wedges. The instances are now removed from `shore_dressing.gd`; the shared curved sand mesh, water mesh, shoreline foam shader and gameplay collision remain in place. The [new initial](images/C06-v20/pier-overhead-initial.png) and [restored](images/C06-v20/pier-overhead-restored.png) overhead views show a continuous shore without the wedges. The [aerial](images/C06-v20/aerial-initial.png) and [player-height along-shore view](images/C06-v20/along-shore-initial.png) retain the thin waterline transition. This improves the local shore artifact but does not resolve the wider C06 water-boundary and palette work below.

Setup/actions/observed: Godot 4.6.1 Compatibility, main playable scene, seed `full-run-integration`, 1920×1080, FOV 85. The paired capture used the same fixed cameras before and after accelerated restoration. It completed 5,400 waste and 300 props, issued one result/receipt, reloaded twice, and reported zero state-invariant failures ([capture log](images/C06-v20/capture-log.txt)). The native pier/shore validation exited 0 with `failures=0`. This change removes only visual instances with no collision or objective ownership.

## Surface facet and shallow-colour pass — 24 September 2026

The Compatibility water material now uses facet colour strength `0.6` instead of `1.4` and shallow colour `(0.58, 0.74, 0.68)` instead of `(0.70, 0.80, 0.65)`. This reduces the oversized light/dark polygon pattern across the bay without moving the shader mesh, changing wave height, or altering the authoritative `WaterVolume` surface. The shallow-colour shift is subtle. In the fixed 1920×1080 aerial view, [before](images/C06-v13/aerial-initial.png) and [after](images/C06-v18/aerial-initial.png) keep the floats and submerged forms visible; the [player-height shore view](images/C06-v18/along-shore-initial.png) keeps the pier, lighthouse and foam edge readable. The water remains brighter cyan than the reference and the nonplayable sand tips still expose a hard background boundary. This is a material pass, not C06 acceptance.

Setup/actions/observed: Godot 4.6.1 Compatibility, `full-run-integration`, isolated profile, identical FOV 85 review cameras in the actual main scene. The v18 paired run rendered the initial and accelerated restored states, completed 5,400 waste and 300 props, issued one result/receipt, reloaded twice and ended with zero state-invariant failures. `tests/validate_faint.gd` exited 0 across submergence hysteresis, air tiers and drop recovery. A 600-frame 1080p water-view profile at `(0,2.1,45) → (0,0,180)` on Ryzen 5 5600 / RTX 3070 measured 6.06 ms mean, 6.47 ms p95, 1.66 ms mean physics, 2,608 maximum draw calls, 190.3 MB scene memory and 1,356.0 MB video memory. This is a developer-machine sample, not the GTX 980-class release gate; the draw count also reflects subsequent reef habitat changes.

Two broader trial changes were reverted after matched renders. Extending both sand and water meshes from ±300 m to ±600 m did not remove the flank boundary. A second plane under the nonplayable flanks created a dark trapezoid behind the beach. Raising underwater fog density from `0.075` to `0.11` washed out nearby coral/debris while retaining the flat horizon. The remaining boundary needs a coherent land/water backdrop, and the reef needs geometry and final art rather than stronger fog.

## Reef-floor tint trial — 24 September 2026

A new-run, starter-air dive at `reef_west:outer` rendered a real, normally aimable can from feet Y -2.35 in 1920×1080 Compatibility. The near seabed read gray in that downward view, so a deep-row vertex-tint trial shifted it toward teal without changing geometry or pickup. The matched render made the floor more uniformly green and farther from the pale sand under blue haze in [reference 06](../plan/references/06-reef.png), so the tint was reverted. The existing [horizontal C06-v4 reef view](images/C06-v4/reef-initial.png) already carries blue distance fog. The next visual pass should address habitat layering and the underwater surface/shore continuity against matched player-height views; recolouring the flat floor alone did not improve the composition. The dive's short trigger prompt and pickup/air result are recorded in [C05](C05.md).

## Shore and offshore water pass — 24 September 2026

The current water mesh now carries the authored `Coastline.shore_z()` value in UV2. Its existing Compatibility shader blends toward a darker offshore colour over 25–190 m from that shore, with a flank term for the nonplayable outer water. The mesh bounds, shore vertices, cosmetic wave settings and gameplay water level are unchanged. Material colours are shallow `(0.7, 0.8, 0.65)`, deep `(0.44, 0.63, 0.64)`, very deep `(0.14, 0.4, 0.42)` and offshore `(0.01, 0.15, 0.24)`; these are shader inputs, not displayed RGB values.

The [C06-v13 aerial initial](images/C06-v13/aerial-initial.png) and [restored](images/C06-v13/aerial-restored.png) views retain visible shallows and make farther water bluer than the [v5 baseline](images/C06-v5/aerial-initial.png). The [along-shore initial](images/C06-v13/along-shore-initial.png) and [restored](images/C06-v13/along-shore-restored.png) views show turquoise player-height water with the pier and lighthouse readable. A fixed initial pixel in that view changed from RGB `(218,255,255)` at v5 to `(66,186,200)` at v13; this is a local comparison, not a palette acceptance metric. The hard transition beyond the nonplayable outer sand tips is still visible in the elevated view. An attempted water perimeter extension produced larger triangular seams and was reverted.

The v13 paired run exited 0 in Godot 4.6.1 Compatibility with seed `full-run-integration`, 1920×1080, FOV 85, 5,400 waste, 300 props, one receipt/result, two reloads and zero state-invariant errors; see the [capture log](images/C06-v13/capture-log.txt). Native `tests/validate_pier_art.gd`, `tests/validate_faint.gd` and `tests/run_checks.gd` exited 0 after the shader edit. The faint pass exercised submergence, surface thresholds, refill and both oxygen tiers. A 600-frame rendered water-filled view at `(0,2.1,45) → (0,0,180)` on the developer Ryzen 5 5600 / RTX 3070 measured 6.04 ms average frame, 6.41 ms p95, 1.66 ms average physics, 1,711 maximum draw calls, 191.1 MB measured scene memory and 1,356.0 MB video memory. This is not the C09 target-hardware/export gate.

## Cloud scale pass — 24 September 2026

The two existing Synty cloud layers are smaller and the upper layer sits 55 m above the first. The [C06-v5 along-shore initial](images/C06-v5/along-shore-initial.png) and [restored](images/C06-v5/along-shore-restored.png) views leave more clear sky around the pier and lighthouse than the [v4 baseline](images/C06-v4/along-shore-initial.png). The [aerial initial](images/C06-v5/aerial-initial.png) shows the same beach and bay underneath. The rendered pair exited 0 with the same content-8 seed, 5,400 waste, 300 props, one receipt/result, two reloads and zero invariants; see its [capture log](images/C06-v5/capture-log.txt). The cloud meshes remain stark white and their layered shapes need further visual review. No gameplay geometry or manifest changed.

## Current result — 24 September 2026

The existing Compatibility underwater environment now uses a bluer background `(0.08, 0.29, 0.42)`, ambient colour `(0.32, 0.56, 0.68)` at energy `0.46`, and fog colour `(0.09, 0.32, 0.47)` at density `0.075`. The shared shore mesh retains its above-water white tint and uses `(0.95, 0.85, 1)` for the near submerged row and `(0.65, 0.75, 0.95)` for deeper rows. Geometry, collision, water level, oxygen logic and item positions are unchanged. The separate `seabed_caustics.tres` is not used by the current beach scene, so it was not changed.

The [C06-v4 reef initial](images/C06-v4/reef-initial.png) and [restored](images/C06-v4/reef-restored.png) captures use the same camera as the [earlier C04-v34 pair](images/C04-v34/capture-log.txt). The near litter remains distinct, the floor is warmer, and the distant boat becomes faint against blue fog. The restored state still exposes sparse upright coral proxies, a flat floor and a hard horizon. The [aerial initial](images/C06-v4/aerial-initial.png) shows that the tint change did not visibly alter the above-water bay composition. The paired capture used Godot 4.6.1 Compatibility, seed `full-run-integration`, 1920×1080 and FOV 85. Its accelerated setup finished 5,400 waste and 300 props, one receipt/result, two reloads and zero state-invariant errors; see the [capture log](images/C06-v4/capture-log.txt).

`tests/validate_world_traversal.gd` exited 0 in the main scene: 147 m route, three huts, both reef lanes, six new catalog items collected by aimed primary input, no overlap among 1,010 reef objective origins, and a real chair carried onto/off the pier. This confirms the changed tint mesh still supports the played route and pickups. It does not establish HUD readability or visual acceptance at 720p.

**C06 remains open.** Tune the above-water light, clouds, foam and shore/deep-water continuity; remove the conspicuous bounded water strips. Inspect water-surface crossings, near-item and HUD readability at 720p/1080p and both FOV extremes with normal input. Profile the expensive water view. C07 owns replacement habitat geometry and plants; the current coral proxies and flat underwater floor cannot pass the reef rubric by colour alone.
