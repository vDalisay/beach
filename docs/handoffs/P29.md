# P29 — Windows candidate export and release integration

Status: **partial handoff / candidate in progress**, 23 September 2026. A Windows x86_64 candidate builds and its packed new-run/save paths pass. Release acceptance is not complete: the human-paced full run, physical controller and 4K pass, clean-machine/profile run, exact open art, and target-hardware performance still require evidence. See [release checks](../release-checks.md) for the gate matrix and SHA-256 hashes.

The current export uses the matching Godot 4.6.3 release template, GDScript runtime and `all_resources` filtering. Vendor source, conversion project, docs, test fixtures, tools and the original unused empty scene are excluded. The executable is `builds/beach-win64.exe` beside `builds/beach-win64.pck` (ignored build outputs). The catalog and Synty wrappers are included. No optional jetski, leaderboard, multiplayer, visitor or sound work was introduced.

The first separate PCK check found the `.tres.remap` catalog-enumeration defect; `ManifestGenerator.load_catalog` now resolves both editor `.tres` and packed `.tres.remap` names to the same resource. After rebuilding, 100 seeds passed from that original PCK. That 4.6.1 PCK started the real full beach at seed `release-pack-smoke`: 5,740 unique records, all 34 definitions, 1,586 nearby views and a loadable Synty drink bottle. An exported-exe headless menu boot exited 0. The packed save flow then passed initial/manual generation, corrupt-latest recovery, failed-write retention, sorting-table state, player pose and Save and quit. A packed physics/save pass covered flying and settled bodies, collection and faint recovery. Both finished `failures=0`; `tests/run_checks.gd` passed again. The save failure fixture's directory error is expected; no real save directory was modified.

Playable setup/actions/observed for this packet: the real `BeachMain.start_run` was instantiated with the final exported PCK mounted as `res://`, the authored `release-pack-smoke` seed entered, and the normal menu start path invoked. It created 5,740 state records and nearby physical item views; the Synty bottle scene loaded from the same PCK. The packed save check then used the full beach scene and actual sorting station, Save and quit, load/recovery and the blocked-write path, ending `failures=0`. These checks do not represent a human-paced complete game or a clean-machine executable playthrough.

User-visible next gate: run the candidate on the intended GTX 980-class / 8 GB Windows system with a physical controller and without a Godot install, record 1080p/4K readability and performance, and complete one unassisted 5,700-object run. The supplied Palm City pack still lacks exact turtle, fish, coral and several tool/rescue models; the full list and current Synty substitutions are in [asset requests](../asset_requests.md). Do not mark functional/visual release-ready from this candidate alone.

After this handoff, the user supplied the two Palm City promotional screenshots as a visual target. The [reference visual pass](reference-visual-pass.md) updated the world art and rebuilt the candidate PCK. The refreshed hash and final-pack reruns are in [release checks](../release-checks.md); the original P29 functional conclusions remain unchanged.

Follow-on isolated-profile boot, 23 September 2026: launched the current exported executable for 120 headless frames with a process-only empty `%APPDATA%` directory, leaving the existing player profile untouched. It exited 0 and created only its fresh `Godot/app_userdata/Beach/logs/godot.log` tree. This is a development-machine fresh-profile boot, not a separate clean-machine install or a standalone new-run/save/controller playthrough. The current PCK's external new-run check also stayed at zero progress/revision for 120 frames after the starting-slot fix; see [release checks](../release-checks.md).

Full-manifest completion check, 23 September 2026: `tests/validate_full_run.gd` instantiated the real beach scene with seed `full-run-integration` and verified 5,740 state records, including 5,400 required waste and 300 required props. As accelerated setup, it cleared dirt and staged 299 props in compatible authored slots, moved waste IDs to the real S1 sorting table in 200-item batches, and released attachments/revealed buried items directly. It then used the real sorting, sealed-bag, bag-hold, container-deposit and truck-collection services for all 5,400 waste IDs. Before the last placement, progress was exactly 5,699 and results/restoration had not fired. Holding and placing the final Synty beach chair through gameplay services opened results, paused the game, restored the starter section and produced a 5,700-item receipt (5,400 waste, 300 props) with no ownership-invariant failures. The completed state saved to a manual slot, reloaded through `BeachMain.load_run`, reopened results with the same serialized receipt and progress, then resumed roaming. Removing the final chair, saving/reloading at 5,699, and replacing it preserved the original receipt and restored habitat without paying again. That sequence found a misleading results panel on incomplete reload; `BeachMain` now presents saved results only while current progress is complete. The check exited 0 in both the workspace and rebuilt exported PCK; `tests/run_checks.gd` and the packed new-run check also passed. This verifies full-state completion and persistence across real services, not a human-paced pickup/cleaning/rescue/transport run or physical-controller playthrough.

Wildlife-facing 4.6.3 candidate follow-on: fish and the returning turtle now face their actual route movement in the live P21 scene. The rendered/headless wildlife checks, 100 packed release seeds, packed 5,740-record Synty-content new run, accelerated 5,700-item completion/save/reload, packed save/recovery, packed physical-item save/recovery, and exported EXE headless boot all exited 0. The save check's deliberately blocked directory emitted its expected error while retaining the previous generation. The 160 m beach and item counts are unchanged. EXE SHA-256: `9635acd1df035f69a2102b500be25a7981cea70d5533d5e5e89c96cd149f67d5`; historical PCK SHA-256: `c12a087ed309155720b0247bc0936a701473b8452111542c51bb375970f077d9`.

Faceted-water candidate: source-pack water normals add low-poly turquoise variation through a project-owned shader variant, leaving the staged Synty source hash intact. The refreshed PCK passed P25 pier/shore and shader reference, the 5,740-record Synty new run, 100 seeds, accelerated full completion/reload, save/recovery and physical-item save/recovery; the EXE booted headless. The 600-frame development-machine 1080p pass measured 10.78 ms p95 with zero near-view gaps, not a target-hardware acceptance result. EXE SHA-256: `9635acd1df035f69a2102b500be25a7981cea70d5533d5e5e89c96cd149f67d5`; historical PCK SHA-256: `38326205f811658b71f0c6480b8da4164203243f88365ac4bf125b3e56c86510`.

Compact-sky candidate: the second instance of the supplied Synty cloud-ring mesh changes scenery only, leaving the 160 m/5,700-required-item run unchanged. The rebuilt 4.6.3 PCK passed pier/sky assembly, 5,740-record Synty new run, 100 seeds, accelerated full completion/reload and physical-item save/recovery; the executable headless boot exited 0. The workspace 600-frame 1080p RTX 3070 sample measured 9.85 ms p95 and zero near-view gaps. A long human-paced and target-GPU/controller playthrough remain open. EXE SHA-256: `9635acd1df035f69a2102b500be25a7981cea70d5533d5e5e89c96cd149f67d5`; historical PCK SHA-256: `a73057883e3f164f292e938ab7f6622e7c662f27742470a66ca4cbd810564066`.

Synty storefront-awning candidate: two batched rows reuse the staged art-deco awning mesh along five city-front buildings. The PCK passed the playable pier/city scene, 5,740-record Synty new run, 100 seeds, accelerated 5,700-item completion/reload and physical-item save/recovery; the EXE booted headless. The 600-frame 1080p RTX 3070 workspace sample measured 7.31 ms p95 with zero near-view gaps. This is a modest street-level visual change, not proof of screenshot-level fidelity or target-device readiness. EXE SHA-256: `9635acd1df035f69a2102b500be25a7981cea70d5533d5e5e89c96cd149f67d5`; historical PCK SHA-256: `504efcb893ebed597d421c3521a1a2142658f19ea8f90c81944cee955543866d`.

Synty promenade candidate: the visible inland walking strip is now covered by source-pack sidewalk tiles, using the unchanged collision body. A too-bright first material was corrected after visual inspection of the live [city-close view](images/reference-city-close.png). Packed P25/new-run/100-seed/accelerated-full-completion/physical-save and EXE boot checks pass; workspace traversal still reaches all 13 waypoints and three hut entrances. The 600-frame RTX 3070 1080p scene sample measured 7.63 ms p95 with zero near-view gaps. Human-paced, controller-only, clean-machine and target-GPU checks remain open. EXE SHA-256: `9635acd1df035f69a2102b500be25a7981cea70d5533d5e5e89c96cd149f67d5`; historical PCK SHA-256: `ec0ef60b7f9050cbab03a1708c8f08c819c829349048dc0a4b060c8ddab6358e`.

Current Synty skyline candidate: the licensed source roof-cap and art-deco balcony meshes are staged locally and visible on collision-free city buildings in the real beach. Asset closure grew to 86 roles / 244 files, with the original Synty water shader hash unchanged. The rebuilt PCK passed the pier/city scene, 5,740-record Synty new run, 100 seeds, accelerated 5,700-item completion/reload and physical-item save/recovery; the EXE booted headless. The 600-frame 1080p RTX 3070 workspace sample measured 7.65 ms p95 with zero near-view gaps. Screenshot-level city fidelity and human-paced/controller/target-hardware acceptance remain open. EXE SHA-256: `9635acd1df035f69a2102b500be25a7981cea70d5533d5e5e89c96cd149f67d5`; current PCK SHA-256: `5fe1ad0531bd667598f7933443576d68827c29c175d265aba6882ce9d32c692b`.
