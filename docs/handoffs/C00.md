# C00 — Reproducible baseline and asset status

Status: **baseline complete** on 23 September 2026. Release remains open. Baseline commit: `f44a5bf` (`chore: record reproducible gameplay baseline`) on `codex/start-implementation`. The commit explicitly contains the project configuration, `Assets/Synty/.gdignore`, project-owned `art/`, `data/`, `scenes/`, `scripts/`, `shaders/`, `tests/`, `tools/`, export preset and historical text handoffs (374 files, `git diff --cached --check` clean). It excludes the licensed `Assets/Synty` source, generated `art/synty`, caches, saves, exports and local historical image/video captures. The committed R02/P05 scale is approximately 160 m; the committed balance chain is cloth → knife → detector → tank before vacuum/sand cleaner.

## Active versions and source

The user reaffirmed **Godot 4.6.1** as the required engine for this completion pass. Exact installed executable: `C:/Users/Home/Downloads/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe`, version `4.6.1.stable.mono.official.14d19694e`; installed Windows release templates are `4.6.1.stable.mono`. Runtime remains GDScript/Compatibility OpenGL. Historical 4.6.3 candidate exports and their results in `docs/release-checks.md` are not 4.6.1 release evidence. The 4.6.3 Jolt overlap warning fix is not being assumed; it must be checked on the 4.6.1 release route.

Current gameplay identity: `beach-content-5`, `manifest-1`, beach version `5`, save schema `1`; 34 item definitions, 5,700 required IDs plus 40 optional valuables, eight zones, 18 sections and three service points. The latest accepted evidence before C00 was the accelerated full-run/PCK and visual work in P29/`compact-beach.md`; the review's F01–F12 remain open until their C packets pass.

The authorized local source prerequisite is `Assets/Synty/POLYGON_Palm_City/` plus `Assets/Synty/shaders/` from the supplied Palm City conversion. Keep `Assets/Synty/.gdignore`. In a fresh authorized checkout, supply those two directories at these exact paths, then run:

```powershell
$beachGodot = 'C:/Users/Home/Downloads/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe'
& $beachGodot --headless --path . --script res://tools/stage_assets.gd
& $beachGodot --headless --path . --script res://tools/stage_ui_assets.gd
& $beachGodot --headless --path . --editor --import
& $beachGodot --headless --path . --script res://tests/run_checks.gd
& $beachGodot --headless --path . res://scenes/main.tscn --quit-after 120
```

**Interface packs, 25 September 2026.** The UI pass ([plan 14](../plan/14-ui-ux-pass.md)) also needs the supplied `INTERFACE_Modern_Menus_SourceFiles_v1.zip`, `POLYGON_Icons_SourceFiles_v3.zip` and `POLYGON_Particle_FX_SourceFiles_v2.zip` in `Assets/Synty/`. `tools/stage_ui_assets.gd` reads the three archives directly and writes 429 files into ignored `art/synty/ui/` (sprites, input icons, cursors; lossless with mipmaps), `art/synty/fx/` (particle textures and three FX meshes) and `art/synty/icon_models/` (38 POLYGON Icons models and their palette). A second run rewrites nothing. `tools/stage_assets.gd --imports-only` leaves `art/synty/ui/` alone so the 2D sprites stay lossless. The pack's open-licensed fonts (Bungee, Barlow Condensed) and the game's own wordmark are committed under `art/fonts/` and `art/ui/`.

**Model and arm build, 24 September 2026.** The project low-poly models in `art/models/*.glb` are committed; rebuild them only after editing `tools/blender/build_models.py`. The first-person arms contain licensed Synty geometry and must be generated locally after staging; without them the game uses the project hand. Run from the project root with Blender 4.4, then re-import with the Godot `--editor --import` step above:

```powershell
$blender = 'C:/Program Files/Blender Foundation/Blender 4.4/blender.exe'
& $blender -b --factory-startup --python tools/blender/build_models.py
& $blender -b --factory-startup --python tools/blender/build_fp_arms.py
```

The staging manifest now has 95 wrapper roles: eight palm/bush variants were added, and the rowboat role's wrapper instances the committed `art/models/rowboat.glb` (`Hull`) and, after the model review, `art/models/rowboat_oars.glb` (`Oars`). It stages 278 files, closure SHA-256 `129b87d06fa704469c56b9a68bd5eae1864dd9fde4fe13a49ae053dceba05fbf` (was `3f614cec…96bc` before the oars). Gameplay content identity is unchanged (`beach-content-8`).

Observed in a separate detached checkout of `f44a5bf`: the two licensed source directories were supplied from the original local conversion by junction, staging exited 0 with **86 wrappers / 244 files** and closure SHA-256 `da0af1ee8498e488aa8d426b1e5aa1d9d297f59a7ce27798b8aa85f9ccbc4612`; 4.6.1 import exited 0; the native check printed `PASS: boot, asset_closure, ownership, input_bindings, manifest` and exited 0; `scenes/main.tscn --quit-after 120` exited 0. This is an actual game entry-scene boot and state check, not a human playthrough. The same checkout had first passed these checks on 4.6.3 before the user selected 4.6.1; those earlier results are superseded for this pass.

## Art register and replacement wiring

`data/asset_manifest.json` names each staged wrapper and source component. `docs/asset_requests.md` maps the **27 split open roles** to runtime consumers and marks exact pack art, explicit substitutes, provisional project art or missing content. Coral and sea plants, vacuum and sand cleaner, flippers and tank, rescue forms and individual missing litter forms are separate requests. A proposed replacement path with a script/no consumer is explicitly unwired; C05/C07/C08 must connect and inspect it in the live game. The gallery's coloured request cubes are not evidence that the same cube renders in gameplay. Thirty-one of the 34 current item definitions use Synty visuals or Synty compositions; ring, net and residue use project-owned visuals.

## Save policy and compatibility observation

Keep `f44a5bf` and its 4.6.1 staging recipe as the compatible source baseline for current content-5 saves. Before C04/C05 change anchors, definitions or generation, copy existing player saves intact, bump the explicit content version and retain the compatible build/commit. The current loader rejects incompatible content without deleting or regenerating a run; no migration framework is planned. Pure art replacements should preserve wrapper paths and the existing content version. C03's schema additions should read old snapshots with deterministic defaults where safe; they cannot invent historical ordering or faint counts.

In the separate checkout, a real 5,740-record content-5 snapshot was written under `user://test_runs/c00`, copied byte-for-byte, and loaded (exit 0). A one-colour edit to `art/first_person_hand.tscn` still loaded that save (exit 0). Temporarily changing the generator and both content-version metadata values to `beach-content-6` rejected it with `Save content is incompatible with this beach build` (probe exit 2), leaving the copied and original save hashes equal. Restoring content-5 loaded the same save again (exit 0). The temporary probe and edits stayed in the verification checkout, not this repository. No existing player save was found in the default `saves` folder; none was modified.

## Open gates

C01–C09 remain. The 4.6.1 export, human-paced full completion, physical controller route, target GPU and final missing art are unverified. Historical 4.6.3 frame timings and PCK results remain useful comparisons but cannot close those gates.
