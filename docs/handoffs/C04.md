# C04 checkpoint — composed destinations and structural reef

## Inland headland contour — 24 September 2026

Two low, sand-toned headland meshes now join the playable city backing to the outer beach between X ±90 and ±258. They are visual-only scenery beyond the X ±80 playable boundary. Their front vertices follow the same inland sand edge through `Coastline.inland_z()`; the existing sand mesh now calls that helper without changing its formula, collision or objective positions. Height rises to about 9 m behind each flank, then tapers to zero at the ends. The initial green ridge treatment created a dark wall from player height and was rejected; the retained contour is lower and uses the existing sand/backdrop palette.

Setup and observed result: Godot 4.6.1 Compatibility, fixed 1920×1080 / FOV 85 cameras, seed `full-run-integration`, isolated profile, playable main scene. The [C04-v56 initial aerial](images/C04-v56/aerial-initial.png) and [restored aerial](images/C04-v56/aerial-restored.png) show continuous land behind both flanks where the [prior view](images/C04-v49/aerial-restored.png) exposed dark gaps. At [sports eye height](images/C04-v56/sports-eye-initial.png) the low west contour stays below the palms and buildings; the [restored along-shore view](images/C04-v56/along-shore-restored.png) retains its occupied seating, pier and lighthouse. The broad sparsely dressed outer land, oversized pier/head relationship and skyline hierarchy still miss the reference, so **C04 composition remains open**.

`tests/validate_pier_art.gd -- --review-pair --review-output=res://docs/handoffs/images/C04-v56` exited 0 after accelerated completion of 5,400 waste, 300 props, one receipt/result, two reloads and zero state-invariant errors; see the [capture log](images/C04-v56/capture-log.txt). `tests/run_checks.gd` exited 0 for boot, asset closure, ownership, inputs and the pinned manifest. `tests/validate_world_traversal.gd` exited 0 after the 147 m shore walk, all three hut entries, shop interaction, both reef lanes, 1,010 reef origin checks with zero rock overlaps, six catalog pickups and a real pier chair carry. The accelerated completion and scripted traversal are bounded evidence, not a paced human run.

The isolated 600-frame dense-view Compatibility profile (`tests/validate_physics.gd -- --profile --c04-dense`) exited 0 with 7.94 ms average / 14.17 ms p95 frame time, 1.35 ms average physics monitor, 4,940 maximum draw calls and 186.8 MB static / 1,356.4 MB video-memory monitors on the Ryzen 5 5600 / RTX 3070. The prior flank-only sample was 7.86/14.09 ms average/p95 and 4,938 draw calls. These short developer-machine samples do not prove target-hardware acceptance; the GTX 980-class exported-build gate remains open.

## Outer bay flanks — 24 September 2026

The nonplayable sand and water surfaces now extend from X ±300 to ±500. Beyond the playable X ±80 shoreline, the coast retreats more slowly (`0.0014` rather than `0.004` times squared flank distance), and the inland sand taper stays at least 6 m wide. The playable shoreline equation is unchanged because its flank distance is zero throughout X -80..80. The water and sand meshes use the same 2.5 m X sampling and shared coast function. No generated objective, destination, section, or content version changed.

In the fixed [initial](images/C04-v49/aerial-initial.png) and [restored](images/C04-v49/aerial-restored.png) aerial views, the near left and right sand tips no longer terminate abruptly inside the frame as they did in the [previous restored view](images/C06-v18/aerial-restored.png). The bay reads as a longer continuous curve. The dark gulf behind the right flank and the abrupt edge of the inland backing remain visible, so the skyline/bay reference row is **still open**. The [restored along-shore view](images/C04-v49/along-shore-restored.png) retains the occupied sports foreground, pier and lighthouse. The provisional reef is unchanged.

Setup and actions: Godot 4.6.1 Compatibility, 1920×1080, FOV 85, seed `full-run-integration`, isolated profile, main playable scene. `tests/validate_pier_art.gd -- --review-pair --review-output=res://docs/handoffs/images/C04-v49` captured matching initial and accelerated restored cameras; [capture log](images/C04-v49/capture-log.txt) reports 5,400 waste, 300 props and zero state-invariant errors. `tests/run_checks.gd` exited 0 for boot, asset closure, ownership, inputs and manifest. `tests/validate_world_traversal.gd` exited 0 after the 147 m walk, all three hut entries, shop interaction, two reef swims, 1,010 reef origin checks with zero rock overlaps, six catalog pickups and a pier chair carry. These are working main-scene checks; the accelerated completion is not a paced human playthrough.

The 600-frame 1920×1080 Compatibility dense-view profile (`tests/validate_physics.gd -- --profile --c04-dense`) exited 0 with 1,209 nearby views, 3,983 batched distant items, 2,568 visible item meshes, 7.86 ms average / 14.09 ms p95 frame time, 1.33 ms average physics monitor and 4,938 maximum draw calls on the Ryzen 5 5600 / RTX 3070. The earlier pre-change sample was 7.84/13.94 ms average/p95; these short samples do not establish a performance change. The GTX 980-class exported-build gate and restored occupied worst-view profile remain open.

## Service entrances — 24 September 2026

The four waste containers at each of S1, S2 and S3 previously formed a line at local Z 5.2 directly across each hut door. In a real new-run approach with a full 20-item bag, the player stopped against S1's organic container at world Z about -1.85 and could not reach the sorting table from the beach. The old traversal check started at local Z 5, already inside that container line. At S1/S3, PMD and organic containers now sit at local X -3.3, Z 5.2/7.2; general and glass sit at X 3.3, Z 5.2/7.2. At S2, the general/glass pair sits at X -5.5 instead to leave the equipment shop approach clear, and the collection phone moved forward to local `(5.5, 0, 7.5)`. All IDs and categories stay the same. The rendered [S1 entrance](images/C05-v4/service-entrance.png) and [S2/shop frontage](images/C05-v4/service-s2.png) show both layouts.

`tests/validate_world_traversal.gd` now starts outside the bins at local Z 10. With normal movement it entered all three huts, reached the S2 equipment shop counter and opened its UI by interact input, walked the 147 m shore route, swam both reefs, collected six catalog forms and carried a real chair on/off the pier; exit 0. `tests/run_checks.gd` and the completed-state `tests/validate_full_run.gd -- --audit-slots --audit-mesh` also exited 0 after the S1/S3 entrance move. The latter retained 5,400 waste, 300 props, one receipt/result, two reloads, 300/300 occupied targets, zero collider/non-shade mesh overlaps and zero parasol contacts. The later S2-specific bin/phone adjustment was checked by the rendered S2 view and the normal shop approach; the full-run audit was not repeated for that visual-only change. All moved stations remain inside the existing service exclusion bounds; the content-8 manifest and slot IDs remain unchanged.

## Current dense-view profile — 24 September 2026

After the final S2 bin/phone position, the playable content-8 beach's dense lounge view ran for 600 rendered 1920×1080 frames with Godot 4.6.1 Compatibility on the development Ryzen 5 5600 / RTX 3070. `tests/validate_physics.gd -- --profile --c04-dense` exited 0 with 1,209 nearby views, 3,983 batched distant items, 2,568 visible item meshes, 2,932 ms initial view build, 7.84 ms average / 13.94 ms p95 frame time, 1.41 ms average physics monitor, 4,938 maximum draw calls, 185.6 MB static and 1,356.3 MB video-memory monitors. The two preceding samples measured 7.96/14.18 and 7.94/14.30 ms average/p95, showing short-run variation. This checks a crowded player-height view; the GTX 980-class exported-build gate and a restored occupied worst view remain open.

## Occupied family walking approaches — 24 September 2026

The completed main scene now passes a controller-driven walking approach to one real occupied target in each of the **15 sorting families**. From a five-metre setup point, the player moves with the normal movement/input path, stops within interaction range and resolves the exact occupied item through the normal aimed interactor. The route chosen for each family is logged as `C04_FAMILY_WALK`; the shallows surfboard needed a closer stop because its low target fell beyond pickup ray range from a 1.8 m stop. The final `C04_FAMILY_WALK reachable=15/15` pass exited 0 after 5,400 waste, 300 props, one receipt/result, two reloads and an invariant-preserving post-result replacement. It used Godot 4.6.1 headless, the playable main scene, seed `full-run-integration` and an isolated profile via `tests/validate_full_run.gd -- --audit-family-walk`.

This covers a representative occupied target per family. The separate 300/300 aimed-target audit covers every slot from nearby points; neither run proves a connected walking route to every one of the 300 positions or a paced human completion. C04 remains open for those broader play checks and the visible coast/skyline composition. A second outer-sand fade trial exposed dark blue flank bands in the aerial view and was reverted.

## Parasol clearance — 24 September 2026

The occupied mesh audit now resolves the 26 parasol AABB candidates from the same 300-prop completed state. The umbrella mesh's narrow shaft is about 0.06 m in radius below its canopy, and the canopy begins above 1.7 m from the mesh origin. For each candidate, the audit requires the neighboring occupied mesh to remain below a conservative 1.65 m canopy floor and outside a 0.1 m shaft margin. All 26 pass; non-shade visual AABB intersections remain zero. The existing [C04-v40 lounge view](images/C04-v40/lounge-pocket-restored.png) shows the real occupied shade groups at player height. This is a mesh-envelope clearance check, not a triangle intersection proof or a substitute for the remaining per-family walking approaches.

Godot 4.6.1 Compatibility, isolated profile, playable main scene, fixed `full-run-integration` seed: `tests/validate_full_run.gd -- --audit-mesh` exited 0 after 5,400 required waste, 300 placed props, one receipt/result and two reloads. It printed `C04_MESH_AABB occupied=300 nonshade=0 shade_candidates=26 shade_contacts=0`. An attempted broader dry-sand flank made two large triangular land wedges in the paired aerial, so that geometry change was reverted. C04 remains open for outer coast and skyline composition, all-slot connected walking routes and a paced occupied-world pass.

## Occupied mesh and pier-rack pass — 24 September 2026

The 300 real prop slots keep their IDs, families and capacities. The occupied mesh audit exposed 39 world-AABB intersections that the collider-only audit missed. Ten came from portable tents, arrival shelf objects and horizontally rotated paddle meshes; three more were edge contacts between pier chairs/surfboards and a pier lounger/shallows lifebuoy. Arrival shelf 02 now has 1.0 m slot spacing; arrival and lounge tent pairs have 3.4 m spacing. Sports and pier paddles use the row basis so their tall meshes stand separately. The pier paddle pool is now on the deck at local `(-3, 1.45, 42)`, rotated so occupied slots face the walkway and empty capture areas sit behind them. The pier surfboards moved 0.6 m inland and shallows lifebuoys 0.5 m seaward. The result has **zero non-parasol world-AABB intersections** among all 300 occupied meshes. The remaining 26 AABB candidates involve parasols shading seats and need direct canopy/pole visual inspection; AABB intersection alone does not prove mesh contact.

The [C04-v40 paired capture](images/C04-v40/capture-log.txt) rendered matching initial/restored 1920×1080 views in Godot 4.6.1 Compatibility, including close [sports paddles](images/C04-v40/sports-paddles-restored.png), [pier paddles](images/C04-v40/pier-paddles-restored.png), [arrival tents](images/C04-v40/arrival-tents-restored.png) and [pier seating/boards](images/C04-v40/pier-seating-restored.png). The paddles stand apart; the pier rack is visibly on the deck; the tents and boards no longer touch their adjacent props in these views. Its accelerated setup completed 5,400 waste and 300 props, one receipt/result, two reloads and zero state-invariant errors. Separate headless `--audit-slots` and `--audit-mesh` passes reached **300/300** occupied targets across 15 families, found zero placed-collider overlaps and zero non-shade mesh AABB overlaps. In the slot pass, controller movement from the pier walkway approached a placed paddle; normal aimed primary input picked it up, previewed its slot and replaced it without changing completion or ownership. The existing lounge and sports chair remove/preview/place/throw sequences also passed. `tests/run_checks.gd`, `tests/validate_world_traversal.gd` and `tests/validate_pier_art.gd` exited 0; traversal retained the 147 m route, three huts, both reef lanes, six catalog pickups and pier chair carry.

**C04 remains open.** The outer sand tips, inland skyline edge and reference composition still need visual acceptance. Per-family normal walking approaches, direct parasol pole/canopy clearance and a paced occupied-world pass remain beyond this representative check. This slot adjustment keeps the content-8 manifest and stable destination IDs; it does not move generated objectives.

## Inland backing contour — 24 September 2026

The city now sits on a narrower, warm sand-coloured backdrop mesh. Its front edge meets the outer beach's inland taper, while the rear slopes below the water around the buildings and tower. The promenade keeps its separate pavement material. The [C04-v34 aerial initial view](images/C04-v34/aerial-initial.png) replaces the former dark rectangular backing with a connected land shape; the [restored aerial](images/C04-v34/aerial-restored.png) uses the same camera. The [along-shore restored view](images/C04-v34/along-shore-restored.png) retains the occupied foreground seating, pier and lighthouse. The rear coastline is still a visibly authored polygon, and both outer sand tips remain angular, so the bay/skyline rubric still fails. C04 remains open.

The content-8 paired capture used Godot 4.6.1 Compatibility at 1920×1080, FOV 85 and seed `full-run-integration`. Its accelerated setup completed 5,400 waste and 300 props, one receipt/result, two reloads and zero state-invariant errors; see the [capture log](images/C04-v34/capture-log.txt). A separate main-scene traversal exited 0 with the 147 m walking route, three huts, both reef lanes, six catalog pickups, no reef origin overlaps and a real pier chair carry. This visual mesh has no collision or manifest change. The paired capture is not a paced completion run; occupied mesh/approach inspection and the remaining reference composition remain open.

## Shore-side sports pocket — 24 September 2026

Two existing sports chair pairs and their parasols now occupy staggered positions near the shore lane at world X about -52 and -36, Z 30–35. Their slot IDs, capacities and owner sections are unchanged. The [content-8 C04-v24 along-shore restored capture](images/C04-v24/along-shore-restored.png) shows real occupied seating and shade in the foreground, with the pier and lighthouse still readable. The [sports eye view](images/C04-v24/sports-eye-restored.png) retains the court-side activity, while the [aerial](images/C04-v24/aerial-restored.png) still exposes the angular outer sand tips and hard inland backing. The paired run exited 0 with 5,400 waste, 300 props, one receipt and two reloads.

The occupied content-8 layout again passed 300/300 target rays across 15 prop families and zero placed-collider overlaps. A separate headless `tests/validate_full_run.gd -- --audit-slots` run exited 0 after controller movement approached both a lounge chair and the new shore-side sports chair, then normal primary input removed, previewed, placed, removed and throw-captured each chair with ownership and progress intact. `tests/validate_world_traversal.gd` also exited 0 with the 147 m route, three huts, both reef swims, six catalog pickups and pier chair carry; `tests/validate_pier_art.gd` exited 0. This adds one more on-foot pocket, but every family/approach and rendered-mesh clearance still need inspection. **C04 remains open** for those checks, the inland edge, outer sand tips and the reference visual rubric.

## Resumed layout pass — 24 September 2026

The current worktree keeps the playable 160 m route and all 300 prop capacities. The pier deck is now 45 m beyond its shore connection, with an 18 m café/head; its module counts, rails, posts, pavilion collision and moorings anchor moved together. A real chair still travels onto and off the deck. Beyond playable X ±80, the shared shoreline bends inland with a capped quadratic taper, and the sand strip narrows to a submerged edge rather than extending broad empty rectangles. The windsurf board and a real arrival chair/lounger/parasol group sit closer to the shore walking view. The lounge's actual 22 chair-pair, 20 lounger-pair and 14 parasol pool nodes now occupy staggered seating pockets across local X -15..23 and Z 9.5..32.5. Pool IDs and capacities did not change.

`tests/run_checks.gd` exited 0 with the content-7 pinned hashes. The current `tests/validate_world_traversal.gd` exited 0: 13 waypoints / 147 m, all three huts, both reef swims, 1,010 reef objective origins with no rock intersection, a normally collected formerly trapped reef can, and a real chair carried on/off the pier. `tests/validate_pier_art.gd` exited 0 after the shortening: 149 pier tiles, 53 rails, solid deck/head collision and a reachable lighthouse marker. A rendered 600-frame dense-lounge profile of the grouped layout on the developer Ryzen 5 5600 / RTX 3070 at 1080p reported 6.63 ms average frame, 12.77 ms p95, 1.60 ms average physics, 3,718 maximum draw calls, 173.0 MB measured scene memory and 1,355.2 MB video memory. This is a developer-machine sample, not the C09 GTX 980 / 8 GB exported-target gate.

The latest [C04-v23 pair](images/C04-v23/capture-log.txt) exited 0 in Godot 4.6.1 Compatibility with 5,400 waste and 300 props completed through accelerated setup, one receipt, two reloads and zero state-invariant errors. The [aerial restored view](images/C04-v23/aerial-restored.png) shows the shortened pier and a visibly curved bay with narrower nonplayable ends. Its outer sand tips are still angular, and the inland city backing remains a hard slab. The [along-shore restored view](images/C04-v23/along-shore-restored.png) shows real foreground furniture, mid-distance windsurf activity and the lighthouse. The [occupied lounge view](images/C04-v23/lounge-pocket-restored.png) avoids canopy clipping and shows grouped seating from the service path, although it is farther from the representative group than the requested close view.

The first full occupied audit found four blocked bucket targets because `shelf:lounges:01` intersected `shelf:sports:04`; moving the lounge shelf made **300/300** occupied pickup rays targetable. A subsequent native shape query found 16 overlapping placed collider pairs: cooler rows with 0.8 m spacing around 1.15 m wide boxes, plus two cross-family pairs. Re-spacing the three cooler rows, moving the arrival row inland and shifting one sandplay parasol cleared them. The final C04-v23 run reports `C04_SLOT_AUDIT targets=300/300 families=15 overlaps=0` after completion and reload. In the representative occupied lounge group, controller movement reaches the chair approach from five metres away; the chair is then removed, placed and removed again with normal primary input, previewed and thrown into the slot capture with normal right-click input. Later save/reload checks passed. The full audit teleports to four 1.8 m approach points per slot and queries actual physics shapes. It does not prove a person can walk to all 300 positions, or that every rendered mesh stays within its collider.

Status: **C04 still open**. On-foot approaches, rendered-mesh intersections, inland edge integration and reference-rubric acceptance remain. The reef rock mesh and flat bed are provisional structural geometry; C06/C07 own final underwater light and habitat fidelity. All paired full-run captures are accelerated visual evidence, not a human-paced completion run. A sloped inland mesh and a smoothstep coast taper were tried in separate captures, but neither improved the visible edges; the retained build uses the prior city ground and quadratic outer-coast taper.

C05's subsequent `beach-content-8` manifest changes were checked against the same occupied layout: `tests/validate_full_run.gd -- --audit-slots` exited 0 with 300/300 targetable occupied props across 15 families, zero collider overlaps, all 5,400 waste and 300 props completed, one receipt/result and two reloads. The same C04 visual and on-foot gaps remain open; see [C05](C05.md).

| Current rubric row | Result | Observed evidence |
| --- | --- | --- |
| Beach-level composition | **Fail** | [Along shore](images/C04-v23/along-shore-restored.png) now has foreground furniture and a readable pier/lighthouse, but the lower frame remains mostly featureless sand. |
| Bay and skyline | **Fail** | [Aerial](images/C04-v23/aerial-restored.png) has a clear curved bay and narrower pier, but the outer sand tips and inland ground still show authored edges. |
| Occupied destinations | **Partial** | [Lounge groups](images/C04-v23/lounge-pocket-restored.png) are visibly staggered; all 300 required props fit, 300/300 pickup rays resolve and no occupied collider pair overlaps. One controller-driven lounge approach passes; other on-foot approaches and rendered meshes remain. |
| Structural reef | **Gameplay pass, visual fail** | Shared rock exclusions and both swim lanes pass; [reef](images/C04-v23/reef-restored.png) still shows provisional pile meshes and flat habitat. |
| Shore colour, water, underwater | **C06 open** | Current captures retain the existing renderer/material baseline; no final light or water acceptance here. |

## Clearance follow-up — 24 September 2026

F13's required-objective obstruction is repaired in the playable build. The generator now uses `ReefDressing`'s actual eighteen rotated box bounds with a 1.2 m clearance margin. It moves a blocked item to the nearest clear cell in its original section, carrying buried reveal positions, piles and rescue attachment pairs together. The land walk route remains clear; an authored pier carry route now also clears large litter from its deck lane after regeneration changed the seed layout. IDs, required counts and section ownership remain intact.

Because these moves alter deterministic manifests, the beach and content versions advanced from 6 to **7** while the save schema and generator label remain 1 and `manifest-1`. Keep commit `3c6b431` for content-6 saves; this checkout intentionally rejects them rather than silently regenerating their original item positions. C05/C07 must reuse these rock bounds and reopen access checks if geometry grows.

Main-scene check, seed `full-run-integration`, isolated profile: `tests/validate_world_traversal.gd` exited 0. It checked 1,010 reef world, buried-reveal and attached origins against the shared bounds, found zero real ReefRock point intersections, aimed at and collected a reef can near the former trapped site through normal primary input, swam both reef channels, walked the 147 m shore route, entered all three huts and carried a real chair onto/off the pier. `tests/run_checks.gd` exited 0, including fixed hashes for the new content version. This closes F13's reproduced overlap; C04's composition, occupied destination audit, capture pair and worst-view profile remain open below.

`tests/validate_full_run.gd -- --final-special` exited 0 in a separate isolated profile: all 5,400 waste and 300 prop requirements completed, with one receipt, two successful reloads and a post-result rearrangement. This remains an accelerated service-driven completion check; it does not replace normal pickup across every reef site or the human-paced C09 run.

`tests/validate_content_economy.gd` exited 0 for three seeds and 3,960 reachable purchase sets; the new clearance move did not change the base-income access model.

Status: **implementation checkpoint, not C04 acceptance**. Paused here at the user's request. Parent build `601cee3` remains the compatible `beach-content-5` code state. This checkpoint uses Godot **4.6.1 stable Mono**, beach definition 6, `beach-content-6`, generator `manifest-1`, and fixed review seed `full-run-integration`. Existing player saves were not removed or migrated. The save loader reports an incompatible beach build for earlier content; use the compatible prior build for those saves.

Subsequent [checkpoint review](../plan/08-checkpoint-review.md), same day: F13 found 23 required WORLD origins inside the new reef colliders, including a frozen can blocked from five pickup-ray approaches. The channel traversal and accelerated completion evidence below remain valid for those specific flows; they do not establish objective access. Resume with the [revised C04 clearance gate](../plan/07-completion-plan.md#c04--compose-the-finished-beach-first) before further visual work. Preserve `3c6b431` as the compatible code for this content-6 checkpoint if the next repair changes generation.

## Authored layout

- Kept all eight zones, eighteen sections, 300 required props, and twenty interchangeable six-slot shelves for the 96 shared small objects. Split the chair, lounger and parasol storage rows into scene-authored pools with stable descriptive IDs. Capacities remain 72 chairs, 54 loungers and 24 parasols: Arrival 8/4/4, Sports 8/0/4, Lounges 44/40/14, Sandplay 0/0/2, Pier 12/10/0.
- Representative lounge pocket: chair pairs around world X -24.5, Z 16; lounger pairs at Z 19; adjacent shade at Z 17.5. Other lounge pairs occupy staggered small groups over local X approximately -12..18 and Z 10..29. Sports chairs were moved around the court at local (-4,24), (4,24), (-9,18), (9,18). The Z 7 service carrying route remains open in the traversal check.
- The visible shoreline and water share `Coastline.shore_z()`. Playable X -80..80 is unchanged; nonplayable tapered flanks extend to X ±300 with six sand dunes and six headland palms. The backdrop mountain scale changed from 14 to 8. The pier geometry and café remain at their earlier locations.
- Both reef areas now contain nine fixed structural forms with conservative box collision (roughly 3.5–4.1 × 1.8 × 2.5–2.8 m). West rock centres span X -6..13, Z 99..131; east spans X 30..53, Z 114..152. The representative west channel at X 2.5, Z 101..119 and east channel at X 40, Z 118..137 were traversed with the real player movement and starter air budget. Rock art is a tinted Synty sand-pile proxy. The C05 objective-fit pass must keep generated items and rescue sites out of the recorded collision bounds.

## Evidence

Setup: main playable scene, seed `full-run-integration`, isolated save root `user://test_runs/c04_review`; accelerated setup places the 300 real prop records through `PlacementService`, sorts and collects all 5,400 required waste records through the stations/containers, saves and reloads the completed run, then continues roaming. The setup lets streamed physics views reconcile before the final placement. It removes, previews, throws into capture and re-places a real chair in the lounge pocket. This is service-driven acceleration, **not human completion**.

Commands run from the project root with `C:/Users/Home/Downloads/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe`:

- `--headless --path . --script res://tests/run_checks.gd` — exit 0; boot, asset closure, ownership, input bindings, manifest.
- `--headless --path . --script res://tests/validate_world_traversal.gd` — exit 0; thirteen waypoints / 147 m, three hut entries, two reef swim channels, real chair carried onto and off the pier. JOY_AXIS_LEFT_Y was injected; no physical controller was used.
- `--headless --path . --script res://tests/validate_pier_art.gd` — exit 0; old lighthouse/pier collision and structure checks.
- `--path . --script res://tests/validate_pier_art.gd -- --review-pair --review-output=res://docs/handoffs/images/C04` — exit 0 under the 4.6.1 Compatibility renderer; `FULL_RUN_COMPLETE waste=5400 props=300 results=1 receipt=1 reloaded=2`. Fixed 1920×1080 SubViewport, FOV 85, five matching camera transforms, at least 180 physics frames per image. Initial and restored invariants each report zero errors. See [capture settings and counts](images/C04/capture-log.txt) and the ten paired PNGs in that directory.
- `git diff --check` — exit 0.

## Reference rubric at this checkpoint

| Row | Status | Observed reason |
| --- | --- | --- |
| Beach-level composition | **Fail** | [Along-shore restored](images/C04/along-shore-restored.png) has a recognizable pier/lighthouse and activity to the left, but water and empty sand dominate the foreground. |
| Bay / skyline | **Fail** | [Aerial restored](images/C04/aerial-restored.png) no longer exposes the nearby rectangular world edge, and the shore is curved, but the broad flanks are sparse and the existing long pier still dwarfs activity. |
| Completed destinations | **Representative pass; full layout open** | [Lounge pocket restored](images/C04/lounge-pocket-restored.png) shows real paired chairs/loungers and shade with an approach. [Sports restored](images/C04/sports-eye-restored.png) has foreground seating. The full beach still reads as repeated seating bands, and individual occupied mesh/access inspection for every family remains. |
| Structural reef | **Gameplay pass; visual fail** | [Reef initial](images/C04/reef-initial.png) and [restored](images/C04/reef-restored.png) show near and middle rock forms and a clear channel. Collision ray probes and both swim routes pass. The uniform proxy rocks, flat bed and provisional habitat do not meet final reef art fidelity. |
| Shore colour/water/underwater | **C06 open** | Captures preserve current lighting/materials for comparison; this packet did not accept C06 treatment. |

The current pair is a matched **C04 build initial/restored** comparison, not a camera-matched comparison with the historical C03 screenshots. UI/hands are hidden only in the composition captures. No HUD-visible human walk/swim recording, physical controller pass, worst-view profile or per-family occupied collision audit was made at this checkpoint.

## Next work when resumed

Finish C04 before C05: first clear F13's trapped objectives and retain shared geometry/exclusion bounds; shorten/recompose the pier and café/head without moving objectives off solid deck; reduce the broad empty flanks and improve along-shore foreground/activity; inspect and adjust all occupied destination meshes and approaches in the playable scene; profile the wide view. Then re-run the same capture pair into a new output directory. C05 must refit its final catalog/clutter, burial and rescue/plant exclusions to those bounds and recheck air-limited pickup routes. Final wildlife/coral art remains a C07 dependency.
