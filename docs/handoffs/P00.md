# P00 — Root project and runnable foundation

Status: complete on 22 September 2026.

## Outcome

- The GDScript project boots through `scenes/main.tscn` with Compatibility rendering and Jolt physics.
- A scalable 1920×1080 native Control menu reserves the root UI layer and exposes boot status, error presentation, and a placeholder run action.
- `BeachMain.start_run()` frees any existing `RunRoot` before creating its replacement; no global inventory, autoload, transition framework, plugin, or C# dependency was added.
- The nested Synty conversion project remains local and outside the root import scan through `Assets/Synty/.gdignore`.
- `tests/run_checks.gd` is the single small native check entry point.

## Files changed

- `project.godot`
- `.gitignore`
- `Assets/Synty/.gdignore`
- `docs/.gdignore`
- `scenes/main.tscn`
- `scripts/main.gd`
- `tests/run_checks.gd`
- `docs/handoffs/P00.md`

The original `node_3d.tscn` is retained and no longer referenced by the project main-scene setting.

## Commands and results

```powershell
$beachGodot = 'C:\Users\Home\Downloads\Godot_v4.6.1-stable_mono_win64\Godot_v4.6.1-stable_mono_win64_console.exe'
& $beachGodot --version
& $beachGodot --headless --path 'C:\Users\Home\Documents\beach' --editor --import
& $beachGodot --headless --path 'C:\Users\Home\Documents\beach' --script res://tests/run_checks.gd
```

- Version: `4.6.1.stable.mono.official.14d19694e`.
- Import: exit 0. Godot detected and ignored the nested `Assets/Synty` project. Documentation is also excluded from runtime imports.
- Native check: exit 0 with `PASS: boot`. It instantiates the configured scene, enters the tree, replaces the placeholder run root, verifies the old root was freed, and frees main.
- Intentional failure validation: temporarily injected a failing `check`, observed `INTENTIONAL_FAILURE_EXIT=1`, then removed the failure and reran the passing check.

## Playable-scene evidence

Setup: launched the configured main scene with Godot Movie Maker at 1920×1080 using Compatibility/OpenGL on an NVIDIA GeForce RTX 3070.

Actions: captured the first rendered frame; exercised `start_run()` twice through the same scene instance in the native check.

Observed: the frame rendered the centred Beach title, subtitle, ready status, and start button without missing resources or script errors. The second start left one attached `RunRoot`, with the first instance already freed.

## Known gaps

- The run root is intentionally an empty placeholder until P02 supplies `RunSession` and P04/P05 supply playable character/world scenes.
- Final menus and art remain owned by P22 and P25.
- Asset staging and gallery validation remain owned by P01; this packet only establishes the import boundary.

No shared contracts changed.
