# C07 checkpoint — local reef fish reward

## Provisional seagrass beds — 24 September 2026

The four reef sections now share 16 small, collision-free seagrass beds placed beside structural rocks. The beds exist before cleanup in a subdued colour; completing a section brightens only its own beds alongside the existing coral and local fish. A partial save reloads that colour directly without replaying restoration. A04P is now recorded as provisional project art in the asset manifest; distinct supplied seaweed and seagrass models remain outstanding. These ribbons are narrow ground cover, not the tall plants or layered habitat required for final C07 acceptance.

Setup: Godot 4.6.1 Compatibility, `scenes/main.tscn`, seed `wildlife-fixture`, isolated `%APPDATA%`. In the existing wildlife validation, the west outer and neighboring coral sections begin dirty. The [matched before view](images/C07-v5/P21-reef-before.png) shows the subdued beds; after accelerated completion of the outer section's 250 real item records, the [matched after view](images/C07-v5/P21-reef-after.png) shows its coral, local fish and greener beds while the neighboring section stays dirty. Reloading that partial state kept only outer-section colour and fish active. With normal input enabled, the player aimed at and picked up a required waste item in the dirty neighboring section, then returned it to the world; state invariants remained valid. This demonstrates one accessible neighbor pickup, not player-paced section completion or exhaustive access.

The first close render showed a bed embedded in the oversized rock visual. Moving the beds to seabed height and beyond the mesh edge corrected that placement. An audit of all 16 bed centers against required world, buried and attached objectives on `wildlife-fixture` found a minimum horizontal distance of 1.41 m. `tests/validate_wildlife.gd` exited 0 in rendered Compatibility mode, `tests/run_checks.gd` exited 0, and `tests/validate_world_traversal.gd` exited 0 with both reefs visited, 1,010 reef items present and zero reef objective overlaps. No item manifest, save format, collision or reward count changed. Other seeds, tall plants, final species art, and the flat seabed/rock composition remain open.

## Section restoration — 24 September 2026

Each reef section now has one fish school parented to its own restoration visuals. Completing that section starts the school alongside the local coral, clarity patch and starfish; the other section's school and the two regional schools remain locked. A restored section resumes its fish after reload. The zone schools still start when the whole reef zone completes. Population keys are stable, and repeated restoration signals do not add duplicate schools.

Setup: Godot 4.6.1 Compatibility, `scenes/main.tscn`, seed `wildlife-fixture`, isolated `%APPDATA%`. In the existing wildlife scene check, the actual west reef starts with both sections unrestored. Completing the outer section through its 250 real item records reveals its local habitat and fish. The [before view](images/C07-v1/P21-reef-before.png) and [after view](images/C07-v1/P21-reef-after.png) use the same submerged player camera and HUD; the zone remains incomplete at the after view. A snapshot loaded at this partial state resumes only the outer school's loop. Completing the coral section then starts the regional schools, and a completed snapshot resumes both local and regional fish. `tests/validate_wildlife.gd` exited 0 with 2 ambient, 4 local and 6 regional schools plus the existing turtle route; no restoration colliders or state-invariant errors were found. This uses accelerated section completion, not a paced collection of 250 items.

`tests/run_checks.gd` exited 0. `tests/validate_full_run.gd` also exited 0 after completing 5,400 waste and 300 props, issuing one result/receipt, reloading twice and rearranging a prop with no invariant errors. The accelerated setup removes thousands of item records between physics frames; its default path now explicitly reconciles the real item views before placing the final chair, as its visual-review and slot-audit paths already did. Before that fixture correction, a plain headless run reported a blocked slot even though the visual paths had passed. This check is not a player-paced completion run.

## Lounge turtle route — 24 September 2026

The existing beach-to-water turtle now unlocks from `lounges`, matching the balance plan. Its authored route starts at world X -2.5, Z 20 in the gap between eastern lounge seating groups, crosses the shore near Z 36 and loops offshore at Z 48–52. It remains one harmless, nonblocking turtle; the restoration flag, route intro and reload behavior still use the existing `PathAnimal` component. No item manifest, save version or objective position changed.

In the main scene's existing wildlife validation, `lounges` begins unrestored and the turtle waits hidden. Triggering the actual lounge zone restoration signal starts its beach walk. After advancing the real tween, its loop is within the water's horizontal bounds and each loop point is above the sand collision and below the water surface. The [underwater route view](images/C07-v2/P21-turtle-route.png) shows the turtle clear of the seabed with the lounge beach behind it. `tests/validate_wildlife.gd` exited 0 in headless and rendered Compatibility runs, with zero state-invariant failures. `tests/run_checks.gd` and `tests/validate_world_traversal.gd` also exited 0; the latter walked 147 m, entered three huts, swam both reefs, found zero overlaps among 1,010 reef objectives, collected six catalog forms and carried a real pier chair. The route's gap was checked against authored seat centres; a paced restored-world walk with every lounge prop occupied remains open.

**C07 remains open.** Rock shapes, coral/plant and animal assets remain provisional; the reef floor and distant horizon are visibly flat. Wider section/zone restoration views, final habitat readability, objective access after final art and player-paced restoration are not accepted by this checkpoint. C04–C06 and C08–C09 also remain open.
