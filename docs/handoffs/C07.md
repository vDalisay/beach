# C07 checkpoint — local reef fish reward

## Taller shared seagrass blockout — 24 September 2026

The 16 existing noncolliding beds now use nine blades each, with varied heights of 0.68–1.52 m. Their positions and dirty/restored ownership did not change: the subdued beds exist beside real litter before cleanup, and the same beds brighten after their section completes. The matched [dirty](images/FIN05-previsual/reef-initial.png) and [restored](images/FIN05-previsual/reef-restored.png) swimmer views show the additional plant height while retaining a clear sandy swim lane. The floor remains broad and flat, and final coral, seaweed and seagrass assets are missing; this is a provisional habitat composition step, not C07/FIN-05 acceptance.

Setup/actions/observed: Godot 4.6.1 Compatibility, `wildlife-fixture` in the playable main scene with isolated `%APPDATA%`. `tests/validate_wildlife.gd` completed the west outer section's 250 records, reloaded its partial restoration, collected a dirty-neighbor item through normal aim/input, and exited 0: 16 bed placements, nearest required item 1.41 m away, zero embedded beds, 2 ambient/4 local/6 regional fish schools and 3 turtle routes. The `full-run-integration` [paired capture](images/FIN05-previsual/capture-log.txt) then finished 5,400 waste and 300 props with one receipt/result, two reloads and zero invariant failures. Blade meshes have no collision and do not alter the manifest, rewards, air or save format. Final-model clearance and a player-paced restored reef swim remain open.

## Rear reef shelf and plant clearance — 24 September 2026

Four existing rear rocks now form a slightly taller, wider shelf: west `ReefRock07–09` and east `ReefRock18`. Their visual scales, solid box colliders and the generator's shared horizontal exclusion bounds use the same 1.4 factor. The neighboring seagrass beds follow those wider bounds. The [matched dirty](images/C07-rear-shelf-final2/P21-reef-before.png) and [restored](images/C07-rear-shelf-final2/P21-reef-after.png) west swimmer views show a higher rear silhouette behind the unchanged sandy lane and local coral reward. This is a bounded structural improvement; the broad foreground floor remains flat and the repeated pile meshes remain provisional.

Setup/actions/observed: Godot 4.6.1 Compatibility, playable `scenes/main.tscn`, seed `wildlife-fixture`, isolated `%APPDATA%`. The existing wildlife flow completed the west outer section's 250 real records, reloaded partial restoration, collected a dirty-neighbor item with normal aimed input, started the zone turtle and exited 0. On that seed, all 16 authored plant beds stayed outside rock collision with 0.25 m margin; the nearest required reef item origin was 1.41 m away. `tests/run_checks.gd` retained the pinned content-8 manifest hash, so these widened bounds did not move the fixture's items. `tests/validate_world_traversal.gd` exited 0 across both swim lanes and 1,010 reef objective origins with zero rock overlaps. No content or save version changed. Other seeds, all item aim paths and the final rock/plant models remain part of C04/C07 acceptance.

## Branching coral blockout — 24 September 2026

The four reef sections' existing 12 coral clusters now use a single low-poly branching mesh per cluster instead of five short straight cylinders. Three cluster heights break up the repeated row, and the existing dirty/restored colour transition still applies to each section. This is collision-free provisional project art; it does not replace the missing coral and seaweed models or solve the flat reef floor.

Setup/actions/observed: Godot 4.6.1 Compatibility, playable `scenes/main.tscn`, seed `wildlife-fixture`, isolated `%APPDATA%`. The [same-camera dirty view](images/C07-branch-final/P21-reef-before.png) shows subdued branching forms around real litter; after the west outer section's 250 required records are completed, the [restored view](images/C07-branch-final/P21-reef-after.png) shows taller mixed-height coral in that section while its neighboring section remains subdued. The existing wildlife scene check then reloaded partial restoration, aimed at and collected a required item in the dirty neighbor through normal input, and exited with `failures=0`. This is accelerated section completion, not a paced station trip.

`tests/validate_world_traversal.gd` exited 0 with both reef lanes, 1,010 reef objective origins clear of rock collision, six aimed catalog pickups and a pier carry. `tests/validate_rescue.gd` exited 0 across 12 rescue sites and 24 attachments. Since only noncolliding coral visuals changed, content and save versions stay at 8. The swimmer view still exposes the flat seabed, repeated rock proxies and missing tall plant species; C07 remains open.

## Mixed structural rock silhouettes — 24 September 2026

Six of the eighteen existing structural reef rocks now use the second approved Palm City sand-pile mesh, tinted with the same reef stone material. Its scale keeps the occupied width, depth and height close to the prior mound; the existing `ReefRock01..18` collision boxes, authored `blocks_point()` exclusions, locations, swim lanes and objective manifest are unchanged. The other twelve retain the original ridge mesh. In matched [before](images/C06-v24/reef-initial.png) and [after](images/C07-v9/reef-initial.png) swimmer views, the repeated sharp peaks now alternate with lower, broader mounds in both reef areas. The [restored view](images/C07-v9/reef-restored.png) still shows a flat foreground and procedural coral, so this is structural variety, not reef acceptance.

Setup/actions/observed: Godot 4.6.1 Compatibility, `full-run-integration` in the playable main scene, fixed 1920×1080/FOV 85 reef camera before and after accelerated restoration. The paired run completed 5,400 waste, 300 props, one result/receipt, two reloads and zero state-invariant failures ([capture log](images/C07-v9/capture-log.txt)). `tests/validate_world_traversal.gd` exited 0 in the main scene after the 147 m walk, three hut entries, normal waterline dive/ascent, both reef lanes, 1,010 reef objective origins with zero rock overlap, six catalog pickups, S1 payment and pier chair carry. The new mound role is declared in `data/asset_manifest.json`; `tools/stage_assets.gd` regenerated 245 files with closure SHA-256 `658956d02f69c79b1505ccb69a0d39a6267c4f0328d40d9dce45b455bcdb5ce0`, and `tests/run_checks.gd` then exited 0 for boot, asset closure, ownership, input bindings and the pinned gameplay manifest. A new `--c07-reef` view in the existing profile script measures the same underwater camera without player movement; 600 frames on Ryzen 5 5600 / RTX 3070 measured 6.03 ms average, 6.39 ms p95, 1.68 ms average physics and 1,794 maximum draw calls. This is a developer-machine new-run sample, not the target-hardware or restored worst-view gate. Final reef species models, real floor relief, player-paced restoration and broader access after final art remain open.

## Reef-zone turtle routes — 24 September 2026

Completing either whole reef zone now starts one persistent, nonblocking turtle water route in addition to that zone's two regional fish schools. The west and east routes use the existing `PathAnimal` and provisional turtle scene, with separate stable population keys `zone:reef_west:turtle` and `zone:reef_east:turtle`. Both loops sit below the water surface and above the structural rocks/seabed. The other reef's turtle remains locked until its own zone completes; a loaded completed reef resumes directly in the loop. The existing lounge beach-to-water turtle is unchanged. Total authored populations are now two ambient, four local and six regional fish schools plus three turtle routes.

Setup/actions/observed: Godot 4.6.1 Compatibility, `scenes/main.tscn`, `wildlife-fixture`, isolated profile. In the existing wildlife check, both reef turtles began hidden. Accelerated completion of west outer then west coral records unlocked west regional fish and its turtle; after advancing the real route tween, the turtle swam in the [rendered west-reef view](images/C07-v8/P21-reef-turtle.png). Physics rays from every west and east loop point found ground at least 0.25 m below the turtle route, while all points remained inside water and below its surface. A snapshot reload resumed only west's turtle; repeated restoration signals created no duplicate routes. `tests/validate_wildlife.gd` exited 0 rendered and headless. This proves the bounded route and latches, not a paced 500-item reef-zone completion.

`tests/validate_full_run.gd` exited 0 after 5,400 waste and 300 props, one result/receipt, two reloads and post-result prop replacement; `tests/run_checks.gd` exited 0 for boot, asset closure, ownership, input bindings and manifest. No objective position, reward amount, save schema or content version changed. The turtles are still provisional art, and a short player-paced swim through both completed reef routes remains open.

## Shared reef coral before and after cleanup — 24 September 2026

The 12 provisional coral clusters in each reef section now stay in the playable habitat before cleanup. Their fixed offsets form loose groups instead of the former 4×3 grid. The dirty state is subdued; completing a section brightens those same clusters while the clarity patch, starfish and local fish appear. A partial reload applies the colour directly, and the neighboring unfinished section stays dim. The shallow-water sections retain their prior reveal behavior. This makes restoration a change to an existing reef, but the blocky procedural coral and sparse flat floor still fail final C07 art acceptance.

Setup/actions/observed: Godot 4.6.1 Compatibility, `scenes/main.tscn`, `wildlife-fixture`, fresh isolated profile. The [before view](images/C07-v7/P21-reef-before.png) shows muted coral with required debris present. The same player camera in the [after view](images/C07-v7/P21-reef-after.png) shows those clusters brighter after accelerated completion of the west outer section's 250 item records; the neighboring section remains dirty. The existing wildlife check also reloaded that partial state, exercised one normal aimed waste pickup across the restored boundary, completed the zone, and found zero state-invariant or population-duplication failures. This is staged completion rather than a player-paced collection and station trip.

`tests/validate_wildlife.gd` exited 0 in a rendered run. `tests/validate_world_traversal.gd` exited 0 with both reef lanes visited, 1,010 reef objectives and zero rock overlaps; `tests/validate_rescue.gd` exited 0 with all 12 sites and 24 attachments. Coral has no physics or interaction collider, and item positions, quota and save format did not change. These checks cover bounded routes and rescue handling; visibility and normal pickup at every coral-adjacent objective remain open.

## Provisional seagrass beds — 24 September 2026

The four reef sections now share 16 small, collision-free seagrass beds placed beside structural rocks. The beds exist before cleanup in a subdued colour; completing a section brightens only its own beds alongside the existing coral and local fish. A partial save reloads that colour directly without replaying restoration. A04P is now recorded as provisional project art in the asset manifest; distinct supplied seaweed and seagrass models remain outstanding. These ribbons are narrow ground cover, not the tall plants or layered habitat required for final C07 acceptance.

Setup: Godot 4.6.1 Compatibility, `scenes/main.tscn`, seed `wildlife-fixture`, isolated `%APPDATA%`. In the existing wildlife validation, the west outer and neighboring coral sections begin dirty. The [matched before view](images/C07-v5/P21-reef-before.png) shows the subdued beds; after accelerated completion of the outer section's 250 real item records, the [matched after view](images/C07-v5/P21-reef-after.png) shows its coral, local fish and greener beds while the neighboring section stays dirty. Reloading that partial state kept only outer-section colour and fish active. With normal input enabled, the player aimed at and picked up a required waste item in the dirty neighboring section, then returned it to the world; state invariants remained valid. This demonstrates one accessible neighbor pickup, not player-paced section completion or exhaustive access.

The first close render showed a bed embedded in the oversized rock visual. Moving the beds to seabed height and beyond the mesh edge corrected that placement. An audit of all 16 bed centers against required world, buried and attached objectives on `wildlife-fixture` found a minimum horizontal distance of 1.41 m. `tests/validate_wildlife.gd` exited 0 in rendered Compatibility mode, `tests/run_checks.gd` exited 0, and `tests/validate_world_traversal.gd` exited 0 with both reefs visited, 1,010 reef items present and zero reef objective overlaps. No item manifest, save format, collision or reward count changed. Other seeds, tall plants, final species art, and the flat seabed/rock composition remain open.

## Section restoration — 24 September 2026

Each reef section now has one fish school parented to its own restoration visuals. Completing that section starts the school alongside the local coral, clarity patch and starfish; the other section's school and the two regional schools remain locked. A restored section resumes its fish after reload. The zone schools still start when the whole reef zone completes. Population keys are stable, and repeated restoration signals do not add duplicate schools.

Setup: Godot 4.6.1 Compatibility, `scenes/main.tscn`, seed `wildlife-fixture`, isolated `%APPDATA%`. In the existing wildlife scene check, the actual west reef starts with both sections unrestored. Completing the outer section through its 250 real item records reveals its local habitat and fish. The [before view](images/C07-v1/P21-reef-before.png) and [after view](images/C07-v1/P21-reef-after.png) use the same submerged player camera and HUD; the zone remains incomplete at the after view. A snapshot loaded at this partial state resumes only the outer school's loop. Completing the coral section then starts the regional schools, and a completed snapshot resumes both local and regional fish. `tests/validate_wildlife.gd` exited 0 with 2 ambient, 4 local and 6 regional schools plus the existing turtle route; no restoration colliders or state-invariant errors were found. This uses accelerated section completion, not a paced collection of 250 items.

`tests/run_checks.gd` exited 0. `tests/validate_full_run.gd` also exited 0 after completing 5,400 waste and 300 props, issuing one result/receipt, reloading twice and rearranging a prop with no invariant errors. The accelerated setup removes thousands of item records between physics frames; its default path now explicitly reconciles the real item views before placing the final chair, as its visual-review and slot-audit paths already did. Before that fixture correction, a plain headless run reported a blocked slot even though the visual paths had passed. This check is not a player-paced completion run.

## Lounge turtle route — 24 September 2026

The existing beach-to-water turtle now unlocks from `lounges`, matching the balance plan. Its authored route starts at world X -2.5, Z 20 in the gap between eastern lounge seating groups, crosses the shore near Z 36 and loops offshore at Z 48–52. It remains one harmless, nonblocking turtle; the restoration flag, route intro and reload behavior still use the existing `PathAnimal` component. No item manifest, save version or objective position changed.

In the main scene's existing wildlife validation, `lounges` begins unrestored and the turtle waits hidden. Triggering the actual lounge zone restoration signal starts its beach walk. After advancing the real tween, its loop is within the water's horizontal bounds and each loop point is above the sand collision and below the water surface. The [underwater route view](images/C07-v2/P21-turtle-route.png) shows the turtle clear of the seabed with the lounge beach behind it. `tests/validate_wildlife.gd` exited 0 in headless and rendered Compatibility runs, with zero state-invariant failures. `tests/run_checks.gd` and `tests/validate_world_traversal.gd` also exited 0; the latter walked 147 m, entered three huts, swam both reefs, found zero overlaps among 1,010 reef objectives, collected six catalog forms and carried a real pier chair. The route's gap was checked against authored seat centres; a paced restored-world walk with every lounge prop occupied remains open.

**C07 remains open.** Rock shapes, coral/plant and animal assets remain provisional; the reef floor and distant horizon are visibly flat. Wider section/zone restoration views, final habitat readability, objective access after final art and player-paced restoration are not accepted by this checkpoint. C04–C06 and C08–C09 also remain open.
