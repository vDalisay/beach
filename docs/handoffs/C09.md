# C09 checkpoint — content-8 Windows candidate

## Separate 4.6.1 export — 24 September 2026

Setup: Godot `4.6.1.stable.mono.official.14d19694e`, the `Windows Desktop` release preset, the current content-8 worktree after the C05 two-bag fix, and the locally staged licensed Synty wrappers. Exported to the separate ignored files `builds/content8-check.exe` and `builds/content8-check.pck`; the older `builds/beach-win64.*` pair was not replaced. The EXE is 104,713,728 bytes, SHA-256 `2b28d11d34a97e55385669fdfd23aa6fe917aafc3672d41fa7be7a80b46dbc80`. The PCK is 31,872,896 bytes, SHA-256 `b2527ea1c6f3cfa9cd5f3f217ffa2295cc33944722343b28563fb496db488e14`.

The first external packed new-run smoke stopped because its historical P29 check still expected 34 catalog definitions. Content-8 deliberately has 40; `tests/validate_release_pack.gd` now checks 40 and prints the actual item/catalog counts on failure. With that correction, the same PCK loaded a fresh 5,740-record run, 40 definitions, 709 nearby physical item views, the Synty bottle/towel/shelter/stick visuals, and no progress or run revision after 120 unattended physics frames. The test script stayed outside the PCK.

The external `tests/validate_release_seeds.gd` check against the PCK generated 100 valid, distinct 5,740-row manifests, reproduced a seed stably, and rejected overlong and control-character seeds. External `tests/validate_full_run.gd` against the PCK completed all 5,400 waste and 300 props, produced one result and receipt, reloaded twice and preserved post-result roaming/replacement; exit 0. External `tests/validate_save.gd` against the PCK passed initial/manual saves, damaged-latest recovery, rack/pose/table state, Save and quit and failed-write retention; `failures=0`. Its blocked-directory error was the deliberately induced failure case. Each run used a separate temporary `%APPDATA%` profile. The exported EXE itself booted with `--headless --quit-after 120` under another empty profile and exited 0 with no stderr.

A one-off external script enumerated the mounted PCK's 890 files. It found zero files under `Assets/`, `docs/`, `tests/` or `tools/`, and no `node_3d.tscn`; the main scene and content-8 straw definition both resolved from the pack. The inspection script was removed after the run. This checks the current candidate's exported resource paths, not a future final export.

This is an interim package check. C04–C08 visual and gameplay gates remain open, so final content and art are not frozen. The packed full run accelerates most pickups through services, and the EXE check is boot only. C09 still requires a final export and package inspection after those changes, physical controller and human-paced completion, exported-build target-hardware/long-session profiling, clean-machine play and final save/recovery verification.

Subsequent C04 outer-palm and C08 carry/remapping edits supersede this candidate. Its hashes and pass results describe the pre-edit package only.
