# Windows release checks — 23 September 2026

Status: **implementation checkpoint, not release-ready**. The current worktree uses the user-selected Godot **4.6.1**, `beach-content-8`, beach version `8`, generator `manifest-1`, save schema `1` (5,700 required and 40 optional IDs). [C03a](handoffs/C03a.md) closes F14/F15; [C04](handoffs/C04.md) closes the reproduced F13 reef obstruction but remains open on composition and full occupied access; [C05](handoffs/C05.md) adds the missing logical waste families and deterministic placement bounds and now has a rendered, scripted normal-input two-bag route through sorting, four bag deposits and $80 collection. Its human-paced and site-visibility gates remain open. Commit `92fb167` preserves the compatible content-7 build. The [remaining plan](plan/07-completion-plan.md) still requires C04–C09 before release acceptance.

The existing executable and pack at `builds/beach-win64.exe` and `builds/beach-win64.pck` were exported with 4.6.3 Windows x86_64 release templates and contain historical content-5 work. They do not include C01–C05 changes or establish a 4.6.1/content-8 release. [C00](handoffs/C00.md) records the separate-checkout 4.6.1 baseline boot; [C05](handoffs/C05.md) records the current playable/catalog evidence and paired captures. Runtime remains GDScript. Every compact-build result, hash and gate table below is historical; human completion, physical controller, final assets and target-hardware/clean-machine acceptance remain open.

An interim [C09 content-8 candidate](handoffs/C09.md) exists separately at `builds/content8-check.exe` / `.pck`, exported by Godot 4.6.1 without replacing that historical pair. Packed new-run (40 definitions), 100 seeds, accelerated full completion/reload and save recovery passed; the standalone EXE booted under an empty profile. The later C04 palm and C08 swim-carry changes supersede this candidate, so a fresh export and packed validation remain open. Human/physical-controller completion, target hardware and clean-machine play are also open. The historical table below describes the older package.

Compact-build update, 23 September 2026: the quarter-length 160 m scene passed workspace boot/asset/ownership/input/manifest checks, 100 distinct release seeds, 148 m authored / 147 m controller traversal with three hut entries, pier/shore collision, 12 rescue sites with 24 cuts, faint recovery, placement, resting-body streaming and full save/recovery. Version 5 spreads loose dry-sand furniture across a wider band while preserving all quotas and Synty scale. Native Synty mesh batching keeps distant trash visible without thousands of resting rigid bodies; the current PCK started a real 5,740-record run with 34 definitions, 555 nearby physical views, the Synty bottle, smaller Synty rolled towel, dedicated Synty shelter item and starter Synty stick shaft. Packed new-run, save/recovery and physics-save checks passed, and the executable headless boot exited 0. An existing version-4 test save was rejected as incompatible rather than loaded at stale coordinates. The latest art pass adds a Synty windsurf board/sail landmark and brighter cyan water at the playable waterline, removes a harsh shadow from the decorative Synty surf-edge mesh, and moves the restored turtle's swim loop fully into water; rendered P25 art, P21 wildlife, P20 completion, 147 m traversal/three hut entries, boot/manifest and packed new-run checks pass without changing the 5,740-record manifest. The user confirmed the existing 160 m beach is the intended quarter-size result. An offscreen 3840×2160 rendered UI inspection found and fixed six starting props auto-snapping into slots before player input; the current packed run remains at zero progress/revision after 120 physics frames, while physical throw-to-slot still passes. Full-manifest completion/save/reload now passes both workspace and current PCK; loading a post-completion save with a removed chair retains the first receipt without incorrectly reopening results at 5,699 progress. The two plain first-person hand capsules are replaced by smaller low-poly forearms/palms/fingers; the rendered 4K HUD and carry/tool checks pass, while an exact Synty hand rig remains unavailable. The restored reef now has a soft-edged sand-clarity tint instead of a cyan rectangle, while the shared low-poly turtle is visually detailed and its returned route clears the actual seabed; rendered P21, packed wildlife and packed full-run checks pass. Fish and the returning turtle now face their path movement; rendered/headless and packed P21, packed new-run and accelerated full-completion/reload, and EXE boot pass with the 160 m beach and 5,700-item goal unchanged. The water surface now uses a restrained moving color facet from the supplied Synty water-normal texture via a project-owned shader variant, leaving the staged shader hash intact; rendered P25/P21, a 600-frame 1080p development-machine stream, packed P25/new-run/100-seed/full-completion/save checks and EXE boot pass. A second staged Synty cloud ring now layers the playable sky without collision or gameplay changes; packed P25/new-run/100-seed/full-completion/save checks and EXE boot pass. Five decorative inland fronts now reuse batched Synty art-deco awnings; rendered P25/147 m traversal/completion, a 600-frame 1080p stream, packed P25/new-run/100-seed/full-completion/save checks and EXE boot pass. A 320-tile Synty promenade surface now covers the former flat walking strip while retaining its collision; current rendered P25/147 m traversal, 600-frame stream, packed P25/new-run/100-seed/full-completion/save and EXE boot pass. The source-pack city roof cap and art-deco balconies now give the decorative skyline more shape, raising the staged closure to 86 roles / 244 files; rendered P25/traversal/600-frame stream, packed P25/new-run/100-seed/full-completion/save and EXE boot pass. Current EXE SHA-256: `9635acd1df035f69a2102b500be25a7981cea70d5533d5e5e89c96cd149f67d5`; PCK SHA-256: `5fe1ad0531bd667598f7933443576d68827c29c175d265aba6882ce9d32c692b`. A human-paced full cleanup and target-hardware performance pass remain open; see [compact beach handoff](handoffs/compact-beach.md).

| Gate | Evidence | Status |
|---|---|---|
| Existing packet handoffs | P00–P29 each have a handoff; `tests/run_checks.gd` passes boot, asset closure, ownership, input bindings and manifest. | Pass for recorded packet checks; P27/P28 manual gates remain open. |
| 100-seed generator | Empty/generated, Unicode, 64-byte maximum, repeated and invalid inputs: 100 distinct valid manifests with 5,740 rows; repeat stable; long/control rejected. Passed both in the workspace and against the final PCK. | Pass |
| Packed new run and Synty content | External `tests/validate_release_pack.gd` loaded the current PCK as `res://`, started `BeachMain.start_run`, found 5,740 records, 34 catalog definitions, 555 nearby physical views, the Synty `SM_Prop_Drink_Bottle_01` bottle, `SM_Prop_Towel_02` towel, `SM_Prop_Shelter_02` tent and visible `SM_Prop_Sign_Pole_01` starter shaft. Progress and run revision remained zero over 120 physics frames without player input. The test script was outside the PCK. | Pass |
| Save integrity with packed resources | External `tests/validate_save.gd` against the PCK: initial/manual generations, damaged-latest fallback, blocked write preserving the old generation, sorting-table save, player pose and Save and quit: `failures=0`. `tests/validate_save_physics.gd` against the same PCK passed thrown-item/bag flight, settled reload, collection and faint recovery with `failures=0`. The deliberately blocked directory prints an expected Godot error. Both write only under `user://test_runs/p24`. | Pass for checked paths |
| Exported executable | Final `beach-win64.exe --headless --quit-after 120` launched separately and exited 0 without stderr. The editor with `--main-pack` exercised new-run logic and packed data, but this is not a substitute for a manual exported-exe playthrough on a clean machine. | Boot pass; standalone playthrough open |
| Package content | `all_resources` export with `Assets/*,docs/*,tests/*,tools/*,node_3d.tscn` excluded. Export log shows Synty bottle and item catalog stored; no excluded-resource `Storing File` entries. `tests/*.gd.remap` and `tools/*.gd.remap` are absent from the PCK file table. The embedded global class cache can still contain those source path strings, which are not shipped files. | Pass for inspected paths |
| Full integrated route | `tests/validate_full_run.gd` completed one 5,740-record beach in both the workspace and current PCK: 299 props staged in authored slots, all 5,400 required waste IDs through the real S1 sorting/bag/container/truck services, then the final Synty chair held and placed through gameplay services. The results receipt was exactly 5,700 required (5,400 waste, 300 props), starter restoration occurred only at completion, and ownership invariants held. The complete state saved and reloaded through `BeachMain.load_run`, reopened results with identical serialized receipt and progress, and resumed roaming. Removing the final chair, saving/reloading at 5,699, then replacing it preserved the first receipt/restoration without a second reward; the incomplete reload no longer shows a misleading results panel. Pickup, rescue, cleaning and transport setup was accelerated. P24 separately exercised failure/recovery. No human-paced full run or uninterrupted state-sequence matrix has been performed. | Accelerated full completion/save pass; manual route open |
| Controller, accessibility and visual art | Synthetic focus/rebinding and 720p/1080p scene captures passed P27; a 3444×1410 live ultrawide capture and a 3840×2160 offscreen render of HUD/booklet/pause passed visual layout inspection. The latter reproduces the project's canvas scaling but is not a physical 4K/TV readability pass. A 9-second silent 1920×1080 clip now shows real carry/throw, Synty-chair snap/group feedback and starter-shore restoration using the gameplay services, with accelerated setup; see P27. The reference-image pass added Synty city façades/glass, mountain, sidewalk, road trim, planter benches, activity-edge palms, cloud ring, shade structures, service-hut wall/roof/window/awning modules, a widened Synty pier-head cafe with outdoor furniture/collision and a thicker modular skyline with eight more Synty shop fronts. The Synty sorting tabletop is exposed around its smaller functional mat. The pass corrected a culled water mesh, removed the hard shallow/deep seam with continuous Synty-textured seabed geometry, and fitted red lighthouse bands in the real beach. The lighthouse now stands on a small Synty sand-mesh/palm island offshore beside the pier, with collision derived from the sand mesh; deep-water tint is lighter. Four fixed Synty table/chair/umbrella cafe sets now dress the sports, lounge and sand-play commercial edges. The compact shoreline is now 160 m, with a 147 m controller-driven route through all three huts; current aerial and eye-height captures are linked from the compact beach handoff. Physical controller-only launch-to-results, physical 4K/TV readability and exact missing wildlife/coral/tool art remain open; see `asset_requests.md`. | Open |
| Target performance | With the current Synty roof cap, balconies, storefront awnings and tiled promenade, a 600-frame rendered 1080p workspace flow on the RTX 3070 development machine measured 7.65 ms p95, 2.61 s synchronous view build and zero near-view gaps. Earlier pack-mounted samples are historical; the new PCK has not had a standalone-exe GTX 980-class / 8 GB RAM route or long-session memory/stall profile. The 4.6.1 Jolt warning was absent in the checked 4.6.3 flows; longer play remains unverified. | Open |
| Clean-profile install | The exported executable completed a 120-frame headless boot with exit code 0 using an isolated empty process-only `%APPDATA%` path; it created `Godot/app_userdata/Beach/logs/godot.log` there and did not touch the existing profile. This confirms fresh-profile boot on the development machine only. New-run/manual-save UI, a separate Windows machine without Godot/.NET SDK and controller detection in that environment remain unexercised. | Partial; external install open |

The first PCK smoke revealed an actual export-only failure: exported catalog entries enumerate as `.tres.remap`, so the old `.tres`-only loader generated an empty catalog. `ManifestGenerator.load_catalog` now strips the remap suffix before loading. The final PCK passed 100-seed generation and the full new-run smoke. This content-loading correction does not change the manifest algorithm or content hashes.

Earlier mixed Synty city-façade build SHA-256: EXE `2b28d11d34a97e55385669fdfd23aa6fe917aafc3672d41fa7be7a80b46dbc80`; PCK `c14215a3facfed92d67d07d60b46d847d2a55aacd159ed04add4b719a3c09fe3`. This historical PCK passed the 100-seed, packed new-run, pier/island/cafe scene and both save checks; the executable booted and exited 0. The 13-waypoint route, all three hut entrances and asset/ownership checks were repeated in the workspace. The warmer villa/deco frontage was inspected from city, sports and aerial viewpoints in the live beach. The selected asset closure was 81 wrappers / 233 files; performance and fidelity remain open.

Curved-shore build SHA-256: EXE `2b28d11d34a97e55385669fdfd23aa6fe917aafc3672d41fa7be7a80b46dbc80`; PCK `1f4780aaf35c800e09ac6ff17b0c7fc6b124ee29765c76130517e651ac2344e6`. The packed 5,740-record new run loaded the Synty bottle; 100 seeds, P25 shoreline/pier scene, save/recovery and physics-save passed; EXE boot exited 0. In the workspace, the full 13-waypoint route, all three hut entries, swim/faint and rescue checks passed. The 1080p development-GPU short sample reached 17.09 ms p95; target performance and promotional-reference fidelity remain open.
